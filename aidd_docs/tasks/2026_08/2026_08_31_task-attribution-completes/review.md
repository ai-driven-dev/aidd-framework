# Review: task attribution completes

- **Verdict**: changes-requested
- **Diff**: `e75b9677...9adb2d35`
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_31
- **Findings**: 0 critical, 7 warning, 5 minor

## Phases

### Phase 1 — An interval reaches what the journal witnessed

- [x] A task declared in a session with no turn end attributes the records that follow it — `cli/src/domain/models/task-attribution.ts:64-85`; run on `cli/dist/cli.js` under a sandboxed HOME: declaration 09:10, write 09:40, no `turn_end`, record 09:20 → `2026_02/2026_02_10_alpha | declared by the flow | $2.00`
- [x] The interval ends at the last moment the journal witnessed, and never later — `task-attribution.ts:68-71,81`; differential run of `e75b9677` vs `HEAD` on the same journal: end moves `09:10 → 09:40` (the write), and a record at 10:30 stays out
- [x] Closing the turn afterwards does not change the attribution — `cli/tests/e2e/telemetry-task-midsession.e2e.test.ts:185-199`
- [x] Each of the three unattributed cases reports its own reason — `task-attribution.ts:128-137`, `cost-report-display.ts:42-46`; built CLI emits `no-declaration`, `precedes-declaration`, `journal-silent` as three distinct rows
- [x] A record earlier than the declaration is never attributed to it — `task-attribution.ts:97` (`>= start && < end`); probe at 09:05 vs a 09:10 declaration → `precedes-declaration`
- [x] Two different reasons present in one period appear as two rows — `cost-report.ts:1008-1030`, asserted at `telemetry-task-midsession.e2e.test.ts:201-214` (all three reasons, distinct rows, one period)

### Phase 2 — Codex declares from its own capture

- [ ] Codex's task fixture is a capture from the running binary, headed with version and date — the repository holds no artefact that distinguishes it from the derivation it replaced: `git diff` on `scripts/__tests__/fixtures/codex-task-declared.json` changes 3 of 12 values (`session_id`, `turn_id`, `transcript_path`), and `scripts/__tests__/fixtures/README.md:100-105` records that the one detail the live capture uniquely revealed (`tool_use_id` shaped `exec-<uuid>`) was deliberately **not** carried into the fixture
- [x] It carries no real session id, prompt, machine path or personal name — sweep over `scripts/__tests__/fixtures/` for `/Users/`, `baptiste`, `lafourcade`, `gmail`, `orca/workspaces`: no hit; paths are `/home/user/probe/…`
- [x] The fixtures README no longer describes a derivation that has been replaced — the "not a fresh capture" paragraph and its changed-field enumeration are removed, `scripts/__tests__/fixtures/README.md:88-105`
- [x] Any remaining derivation whose tool is runnable is named — `opencode-session-created.json`, named at `scripts/__tests__/fixtures/README.md:318-323`
- [x] The declaration reader extracts the path Codex's own capture names — `scripts/__tests__/aidd-telemetry-task-declaration.test.js:39-44`; `node --test` → 43/43 pass
- [x] Renaming the wrapper in the capture turns a test red — `aidd-telemetry-task-declaration.test.js:73-83`, all five hosts including `codex` and `opencode`

### Phase 3 — OpenCode's answer is measured, and bounded

- [x] The probe records every delivered event and its part type — recorded at `scripts/__tests__/fixtures/README.md:325-336` (`message.part.updated`, `part.type: "tool"`, three state transitions)
- [x] No more than three further sessions were run — `scripts/__tests__/fixtures/README.md:349-352`, two of the three used, one held in reserve
- [x] If a tool part arrived, OpenCode declares a task from a captured payload — `plugins/aidd-telemetry/hooks/opencode-plugin.js:72-83`, `scripts/__tests__/fixtures/opencode-tool-part-completed.json`, asserted `scripts/__tests__/aidd-telemetry-opencode-payloads.test.js:139-163`
- [x] If none arrived, the outcome names what ran, what arrived, what did not, and the date — not-applicable: a part arrived; the superseded negative is still recorded with its date at `README.md:337-346`
- [x] Nothing states that tool parts are undelivered on the strength of not having seen one — `README.md:342-346` states the inverse explicitly; `session.created`'s own measured-not-observed framing is preserved at `opencode-plugin.js:45-48`
- [ ] Every coverage table names all five tools — `aidd_docs/tasks/2026_08/2026_08_31_core-capture-matrix.md:131` still reads "OpenCode ✗ — `telemetryTaskAttributable: false` (`opencode.ts:202`), the only tool so declared. No tool-call event ever reaches the journal", false at `cli/src/domain/tools/ai/opencode.ts:205`

## Findings

| Sev | Kind | Phase | Location | Issue | Fix | Outcome |
| --- | ---- | ----- | -------- | ----- | --- | ------- |
| 🟡 | code | 1 | `cli/src/domain/models/cost-report.ts:543-547` | The justification gating the fallback is false. It claims a `task_declared` path was "already matched against this same folder pattern", but `plugins/aidd-telemetry/hooks/lib/file-writes.cjs:22` has no `..` rejection while `cli/src/domain/models/task-identity.ts:35` rejects any path containing `..`. Proven on the built CLI: journal `task_declared path="aidd_docs/tasks/2026_02/2026_02_10_a..b/spec.md"` + `turn_end` 09:30, record 09:20 → `the journal falls silent before this record`, for a record squarely inside a witnessed declared interval | Align the reader's `..` guard with the hook's own gate, or give the unparseable-path case its own branch instead of borrowing a reason that asserts a fact about the journal it has not checked | **Fixed**, both layers. `taskIdentityFromWrittenPath` now rejects only a literal `..` path segment (`writtenPath.split("/").includes("..")`), never the substring — the exact repro (`2026_02_10_a..b`) now resolves to a real task. `buildTaskIntervals` also now refuses to *emit* an interval for a declared path that still fails identity resolution (a genuine climb, e.g. `../etc/passwd`), so a genuinely-unusable declaration can no longer widen the *previous* interval nor get read as "journal-silent" — it correctly folds into `no-declaration` instead. The `cost-report.ts:543-547` comment is rewritten to state this is unreachable for every interval this codebase's own wiring produces (not in the type `declaredTaskKeyOf` actually takes — `CostReportSessionJournal.taskIntervals` is a plain input field a test can still hand an unresolvable `path` directly, which is why the fallback stays). Built-CLI proof in the executor report. |
| 🟡 | code | 1 | `cli/src/domain/models/task-attribution.ts:132`; `cli/src/infrastructure/adapters/run-journal-reader-adapter.ts:129-133` | A real `task_declared` line whose `at` does not parse is dropped by `timed()`, so `intervals` is empty and the report prints **"no task was ever declared in this session"** — false; the journal did declare. The adapter accepts any string `at`, so this is reachable, not theoretical. Proven on the built CLI | Either count an undroppable-but-unparseable declaration into `read.unreadable_lines` and label the row "no readable task declaration in this session", or reject the line at the adapter so the count is honest | **Fixed**, relabel option (not the `unreadable_lines` route: that counter is fed from the sink adapter's `skippedLines`, a different data source with no run-journal equivalent — folding one into the other would misuse it). `TASK_UNATTRIBUTED_LABELS["no-declaration"]` now reads "no usable task declaration in this session" (deviates slightly from the review's suggested "no readable…" — same intent, noted here rather than silently). True whether nothing was ever declared or a declaration exists but cannot be placed in time, matching `CostReportTaskRow`'s own pre-existing doc ("the journal records a task_declared line or it does not, and those two read as `no-declaration` alike"). Doc comments, the skill's report template, and the e2e assertion updated to match. Built-CLI proof in the executor report. |
| 🟡 | code | 3 | `plugins/aidd-telemetry/hooks/opencode-plugin.js:33,72-83,110-115` | Every completed tool part now triggers a synchronous `spawnSync("node", [journal.cjs, "tool-used"])` inside OpenCode's in-process `event` handler. Before this diff the plugin spawned at most once per session (`session.idle`). No comment weighs the added per-tool-call latency, and unlike every other host the spawn blocks the agent's own event loop rather than the host's hook runner | Pre-filter on the serialised `part.state.input` with the same task-path regex the hook already owns before spawning, or state the cost where the join is made | **Fixed.** `declaredTaskCallFor` now tests `part.state.input` against a `TASK_PATH_PATTERN` mirroring `task-declared.cjs`'s own before building a call, refusing (never spawning for) any tool part that names no task path. Duplicated rather than imported — this file's own top comment already measured that OpenCode's loader cannot see a local CommonJS file's exports at all, the same reason it spawns `journal.cjs` as a child process instead of calling its functions in-process; `jscpd` does not scan `plugins/`, so this duplication carries no tooling cost. Verified lossless for OpenCode specifically: `journal.cjs`'s `tool-used` dispatch also runs `handleFileWritten` and `handleStepStart`, but both are unconditional no-ops for OpenCode (`writtenPath: null`, `stepStart: null` in `lib/tools/opencode.cjs`), so a call this filter refuses would have done nothing after the spawn either. Updated the one existing test that pinned the old "converts to a call regardless" behaviour. |
| 🟡 | rot | 2 | `scripts/__tests__/fixtures/codex-task-declared.json`; `scripts/__tests__/fixtures/README.md:88-105` | The fixture carries nothing a re-synthesis of the retired derivation would not: 3 of 12 values changed, key set and every other value byte-identical. The one field the README says the live capture uniquely revealed (`tool_use_id` = `exec-<uuid>`) was deliberately excluded, so no artefact of the capture survives — while the paragraph that used to disclose exactly this indistinguishability ("a plausible invention could not be told apart from a genuine one by inspection alone") was deleted in the same commit | Carry the captured `tool_use_id` shape as phase-2 task 1.3 asks ("key set exactly as captured, synthesise every value"), or keep a disclosure sentence saying the fixture is not self-evidencing | **Fixed, both halves — no new live capture attempted.** A live re-capture was considered and rejected: `aidd setup --ai codex --plugins aidd-telemetry` touches user-global `~/.codex`, and this session's hard constraints put avoiding any global state change ahead of closing a warning-level finding. Instead: `codex-task-declared.json`'s `tool_use_id` now carries the `exec-<uuid>` shape the live run uniquely revealed (still a synthesised value, per this set's own rule — every value here is synthesised). `fixtures/README.md`'s paragraph is rewritten to disclose plainly that the committed file, even with the shape carried, is not self-evidencing — the exact uuid is invented, so inspection alone cannot distinguish a genuine capture from a careful re-synthesis — and points at commit `ead9a2f3`'s own message as where the live-run evidence actually lives. |
| 🟡 | rot | 3 | `aidd_docs/tasks/2026_08/2026_08_31_core-capture-matrix.md:129,131` | The capture matrix's task dimension still declares OpenCode unable to declare (`telemetryTaskAttributable: false`, "No tool-call event ever reaches the journal") and Codex's task payload "hand-written". Both are false at `HEAD`; the file was committed one commit earlier (`e75b9677`), same day, and is cited as current state by `2026_08_31_backlog-state.md` | Update the OpenCode and Codex rows of dimension 4, or date-stamp the matrix as a snapshot so it stops reading as current | **Fixed — rows corrected, not date-stamped** (a date-stamped table would still state the opposite of shipped behaviour, which the Phases section's own unmet criterion rules out). OpenCode's row now reads `telemetryTaskAttributable: true`, settled by measurement, with Captured/Tested/Validated/Normalised all updated. Codex's Validated cell now says "real, live capture," not "hand-written payload." The ranked-findings section's own stale "Task, on Codex / Copilot / Cursor" item is also corrected to name only Copilot/Cursor as still hand-written. |
| 🟡 | functional | 2 | `scripts/__tests__/fixtures/codex-task-declared.json` | Criterion "Codex's task fixture is a capture from the running binary" is not corroborated by anything in the repository (see the `rot` row above) | Land the captured `tool_use_id` shape, or restore the disclosure that the fixture cannot be told from a derivation by inspection | **Fixed** — same change as the `rot` row above: `tool_use_id` shape carried, disclosure restored. |
| 🟡 | functional | 3 | `aidd_docs/tasks/2026_08/2026_08_31_core-capture-matrix.md:131` | Criterion "Every coverage table names all five tools, with what each can and cannot declare" is unmet: this table names all five but states the opposite of the shipped behaviour for OpenCode | Correct dimension 4's OpenCode row | **Fixed** — same edit as the `rot` row above. |
| 🟢 | code | 1 | `cli/src/domain/models/task-attribution.ts:97` | A record whose moment is exactly the journal's last witnessed instant reads "the journal falls silent before this record" although the journal has a line at that very instant. Proven: write at 09:40, record at 09:40 → `journal-silent`. Half-open `[start, end)` is the documented convention, the label is not | Word the label for the half-open convention ("the journal's declared coverage ends at or before this record"), or leave as a known one-instant edge | **Not touched** — out of the scope this dispatch enumerated (findings 1–6 above and 6a/6b/6c below only); left as the known one-instant edge, unchanged. |
| 🟢 | fit | 1 | `cli/src/domain/models/task-attribution.ts:35-42,58-62`; `plan.md` decision row 2 | `timed()` filters only `NaN`, so a `file_written` line dated in the future makes the interval effectively unbounded — proven: a write dated `9999-12-31` attributes a record in 2040, and one at the max JS date attributes year 200000. Not a regression (the same hole existed through `boundaries`), but the widening enlarges the surface from a handful of boundaries to every write, and neither the plan's "still never runs open-ended" row nor the restated doc comment names it | Clamp `lastMs` to the report's own period end, or name the clock-skew residual where the cap is justified | **Fixed.** `buildTaskIntervals` takes an optional `periodEndMs` and clamps the witnessed last moment to it; `ReportCostUseCase` computes it from `options.period.toDay` (the exclusive start of the day after) and threads it through `toSessionJournal`. Safe for every record this report considers: no record `readRecordsInPeriod` could ever return falls past that moment, so the clamp changes nothing for any record actually reportable — it only removes the unbounded magnitude a clock-skewed `file_written` line could otherwise produce. (Not a universal property of `buildTaskIntervals` itself — a caller could in principle pass a `periodEndMs` narrower than what it later checks moments against; this use case's own wiring never does.) Two new unit tests cover the clamp and confirm a real, in-period closer is never pulled earlier. `plan.md` decision row 2 is not re-edited — the residual is now named in `buildTaskIntervals`'s own doc comment instead, which supersedes it. |
| 🟢 | conform | 3 | `plugins/aidd-telemetry/README.md:138-139`; `scripts/__tests__/aidd-telemetry-task-declaration.test.js:5-11` | Both state "one real, live capture per host", but `opencode-task-declared.json` is not a captured payload — it is the call `opencode-plugin.js` builds from a captured event, as `scripts/__tests__/fixtures/README.md:60-66` correctly says. Three statements of one fact, one weaker than the other two | Say "captured event" for OpenCode in both places, or point them at the fixtures README rather than restating | **Fixed**, both locations reworded to distinguish OpenCode's four siblings (captured live) from its own payload (the call the plugin *builds* from a captured event), matching the fixtures README's own correct phrasing. |
| 🟢 | code | 3 | `cli/tests/domain/tools/registry-conformance.unit.test.ts:246` | `hookReachesToolUse = host !== undefined` reduces the assertion to `telemetryTaskAttributable === (telemetryJournalHost !== undefined)`, identical to `journalAttributable`. The comment still claims it pins "a fact about that one file this side cannot import"; it now pins nothing host-specific, and a future host that journals without dispatching tool-used would pass silently | Pin the tool-used dispatch set by name, or drop the claim from the comment | **Fixed** — comment corrected, assertion left as-is. `opencode-plugin.js` is an ESM file this suite does not import (unlike the `.cjs` lib files reached via `createRequire` elsewhere in this file), so pinning its dispatch by name from here was not cheaply available; the comment now states what the assertion actually verifies — `telemetryTaskAttributable === (telemetryJournalHost !== undefined)`, true because task declaration has no per-host gate once a host's events reach the journal at all — and points at `aidd-telemetry-opencode-payloads.test.js` as where OpenCode's own dispatch is exercised live. |
| 🟢 | rot | 2 | `plan.md` decision row 5 | "Nothing else in the set is derived while its tool is runnable — that is the standard" is false as written: `opencode-session-created.json` is a declared derivation (`fixtures/README.md:318-323`) and `opencode 1.14.20` is on PATH and was run for this very deliverable. The real standard is narrower — a derivation stands where the *event* was never observed — which the fixtures README states correctly and the plan does not | Restate the plan's standard as the README's: a capture replaces a derivation where the *event* can be produced, not merely where the binary runs | **Fixed** — `plan.md` decision row 5 restated: a derivation stands where the *event* was never observed, naming `opencode-session-created.json` explicitly as the case that stays a derivation despite `opencode` being runnable. Only this row touched. |

## Verification

| Metric        | Value                                             |
| ------------- | ------------------------------------------------- |
| Verified      | 89% (16/18)                                       |
| Files checked | `cli/src/domain/models/task-attribution.ts`, `cli/src/domain/models/cost-report.ts`, `cli/src/domain/models/cost-report-envelope.ts`, `cli/src/application/display/cost-report-display.ts`, `cli/src/application/display/cost-report-artefact.ts`, `cli/src/domain/tools/ai/opencode.ts`, `cli/src/domain/tools/contracts.ts`, `cli/src/domain/models/step-attribution.ts`, `cli/src/domain/ports/run-journal-reader.ts`, `cli/src/infrastructure/adapters/run-journal-reader-adapter.ts`, `cli/tests/e2e/telemetry-task-midsession.e2e.test.ts`, `cli/tests/domain/models/task-attribution.unit.test.ts`, `cli/tests/domain/tools/registry-conformance.unit.test.ts`, `cli/tests/fixtures/cli-owns-read/expected-envelope.json`, `plugins/aidd-telemetry/hooks/opencode-plugin.js`, `plugins/aidd-telemetry/hooks/lib/file-writes.cjs`, `plugins/aidd-telemetry/hooks/lib/task-declared.cjs`, `plugins/aidd-telemetry/README.md`, `plugins/aidd-telemetry/skills/01-cost/actions/03-report.md`, `scripts/verify-chain.mjs`, `scripts/__tests__/fixtures/README.md`, `scripts/__tests__/fixtures/codex-task-declared.json`, `scripts/__tests__/fixtures/opencode-tool-part-completed.json`, `scripts/__tests__/fixtures/opencode-tool-part-no-task.json`, `scripts/__tests__/fixtures/opencode-task-declared.json`, `scripts/__tests__/aidd-telemetry-task-declaration.test.js`, `scripts/__tests__/aidd-telemetry-opencode-payloads.test.js`, `scripts/__tests__/aidd-telemetry-cost-skill.test.js`, `aidd_docs/product/cost-report-contract.md`, `aidd_docs/product/metrics-contract.md`, `aidd_docs/tasks/2026_08/2026_08_31_core-capture-matrix.md`, `docs/ARCHITECTURE.md` |
| Unchecked     | Codex's task fixture is a capture from the running binary — fix; Every coverage table names all five tools — fix |
| Unplanned     | `scripts/verify-chain.mjs:711-724` (`TASK_DECLARED_EXPECTATION` opencode → `possible: true`) and `docs/ARCHITECTURE.md:53`, projected by no phase — justified, both are the same OpenCode fact phase 3 task 4 requires kept consistent; `cli/tests/domain/tools/registry-conformance.unit.test.ts:237-247`, projected by no phase — needed for the `telemetryTaskAttributable` flip to typecheck, but it weakened the assertion (minor finding above); `cli/tests/fixtures/cli-owns-read/expected-envelope.json` `task_attributable false → true`, correct and forced by the same flip |
