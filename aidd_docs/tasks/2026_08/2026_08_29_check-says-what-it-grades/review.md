# Review: the diagnostic says what it is grading

- **Verdict**: blocked
- **Diff**: `05d346f1...HEAD` (2e7632b2, d0751397, ae6dde28, dc0db32a, a048690e)
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_29
- **Findings**: 3 critical, 6 warning, 5 minor

## Phases

### Phase 1 — what is in place, before any verdict

- [x] Every stated fact names the location it came from — `cli/src/application/display/telemetry-check-display.ts:47-63`; run on a sandboxed HOME prints four rows, each naming a path or `AIDD_TELEMETRY`
- [x] Nothing stated is a count or a figure — `telemetry-setup.ts:10-13`; the four rendered rows carry only paths and yes/no. The one count (`${journals.length} run file(s)`) lives in `telemetry-claim.ts:205,228`, on the claims side, and pre-dates this diff
- [ ] A location that cannot be read says so, and every other stated fact still appears — `TelemetryRecorderDeclarationSetup` (`telemetry-setup.ts:102-109`) has no `readable`. `manifestDeclaresPlugin`/`settingsDeclaresPlugin`/`hooksDeclarePlugin` (`telemetry-evidence-adapter.ts:100-154`) all collapse unreadable into `return false`. Measured: a `.claude/settings.json` that **does** declare the recorder but has a trailing comma, or a `//` comment, or mode `000`, prints `recorder declared  nowhere this build checks` and grades `hook fired  FAIL`. Same for a damaged `.aidd/manifest.json`
- [x] A machine where nothing has ever been measured states what is in place, with no failure reported — measured on a fresh sandbox HOME: four stated rows, then the gate line, no `FAIL` among the stated facts
- [x] A person's own refusal reads as theirs, distinct from a project nobody switched on — `telemetry-check-display.ts:49-51`; measured `AIDD_TELEMETRY=0` → `no — this person's own refusal (AIDD_TELEMETRY)`
- [x] Measurement being off does not reduce the output to one line — `diagnose-telemetry-use-case.ts:110-113` gathers `setup` before the gate; measured 5 lines with the switch off

### Phase 2 — "not yet" stops being a failure

- [x] A project switched on with the recorder declared and no session reports nothing to evaluate — `telemetry-claim.ts:155-165`; measured `hook fired  --  no run file in aidd_docs/runs yet — nothing to evaluate`
- [x] Its detail states that a declaration is not proof the recorder will fire — `telemetry-claim.ts:161-163`
- [x] A project with the recorder declared nowhere reports a failure naming the recorder — `telemetry-claim.ts:167-180`; measured `FAIL ... the recorder is declared nowhere this build checks`
- [x] The claim count and order are unchanged — `telemetry-claim.ts:20-24` untouched; four claims in the same order in every measured run
- [ ] Every case that already had a run file keeps its previous verdict — a run file that exists but carries no session anchor (`session.vendor_id` absent: torn file, or `SessionStart` lost while `PostToolUse` wrote) is filtered out by `sessionJournalsOf` (`telemetry-claim.ts:110-114`) and falls into `noRunFileClaim`. Measured with one such file present: recorder undeclared → `FAIL`; recorder declared → `--  no run file in aidd_docs/runs yet — nothing to evaluate`. Before this diff that path was `fail`/`hook-never-fired` (`git show 05d346f1:cli/src/domain/models/telemetry-claim.ts`). The e2e that asserts this criterion (`telemetry-check.e2e.test.ts:554`) seeds a **well-formed** journal only, so the regressing path is unasserted
- [x] An untrusted Codex hook still explains the absence ahead of either new reason — `telemetry-claim.ts:186` orders `trustExplainsAbsence` first; `telemetry-claim.unit.test.ts:180-190`

### Phase 3 — the consumers of the shape follow it

- [ ] The skill's account of each verdict matches what the command prints — `02-diagnose.md:52-56` states "a `config.toml` with no trust table at all falls back to the declared/nowhere reading rather than a guess at trust". Measured with `CODEX_THREAD_ID` set and a `~/.codex/config.toml` that exists but approves nothing: `hook fired FAIL  Codex has not trusted this plugin's hook`, the `untrusted-codex-hook` reading, not the declared/nowhere one — `parseHookTrust` (`hook-trust-reader-adapter.ts:37-43`) returns `trusted: false` for a readable file, and `trustExplainsAbsence` (`telemetry-claim.ts:128-130`) takes it. Only an **absent** `config.toml` falls back, which is what the same file's own Test table row says — the prose and the table give the same outcome for two different conditions, and only the table is true. Every other verdict account matches
- [x] The skill tells the agent to relay what is in place, not only the claims — `SKILL.md:32`, `02-diagnose.md:24-29`
- [ ] A guard fails when the skill and the command disagree about a verdict — the guard (`telemetry-claim.unit.test.ts:420-457`) asserts three literal phrases exist **somewhere in the whole file**. Mutation: rewriting `02-diagnose.md:42` from ``reads `--`, nothing to evaluate, never `FAIL` `` to ``reads `FAIL`, a broken install, never `--` `` — the skill now teaches the opposite verdict — leaves 33/33 green. Only deleting every occurrence of a phrase (4 sites) trips it
- [x] The generated catalogue and prompt index match what their scripts produce — re-ran both `scripts/summarize-markdown.js` invocations from `lefthook.yml:90,104`; `git status --porcelain` stayed empty

## Findings

| Sev | Kind | Phase | Location | Issue | Fix | Outcome |
| --- | ---- | ----- | -------- | ----- | --- | --- |
| 🔴 | functional | 1 | `cli/src/domain/models/telemetry-setup.ts:102-109` | The recorder-declaration fact carries no "could not be read". A present-but-unreadable declaring file (trailing comma, `//` comment, `chmod 000`) reads `nowhere this build checks` and grades `hook fired FAIL`. It violates spec "a location that cannot be read says so and costs only itself" and "an absence that has two causes is never reported as one of them", and reinstates the exact failure the spec was written against: a healthy install told it is broken | Add `readable: boolean` (or a per-location read outcome) to `TelemetryRecorderDeclarationSetup`; have the three readers in `telemetry-evidence-adapter.ts:100-154` distinguish ENOENT from a read/parse failure the way `readSwitchFile:76-95` already does; render `could not be read — <path>`; and make `TelemetryEvidence.recorderDeclared` a tri-state so `noRunFileClaim` never grades `FAIL` on an unread file | fixed — `TelemetryRecorderDeclarationSetup` gained `unreadable: readonly string[]`; `TelemetryEvidence` gained `recorderDeclarationReadable: boolean`; `noRunFileClaim` grades a new `recorder-declaration-unreadable` reason at `unknown` (`--`), never `fail`. Verified on the built CLI, sandboxed HOME, before/after: before, a `.claude/settings.json` with a trailing comma read `recorder declared nowhere this build checks` and `hook fired FAIL … the recorder is declared nowhere this build checks`; after, `recorder declared could not be read — <path>` and `hook fired --  … whether the recorder is declared could not be read`. Tests: `diagnose-telemetry-use-case.unit.test.ts` "reads unknown, never a failure…"; e2e "says the declaration could not be read, never that the recorder is missing…" |
| 🔴 | functional | 2 | `cli/src/domain/models/telemetry-claim.ts:232-236` | A run file that exists but has no session anchor now reads `--  no run file in aidd_docs/runs yet — nothing to evaluate` when the recorder is declared; it read `fail` before this diff. The recorder demonstrably fired (it wrote the file) and the detail says the opposite. A recorder writing files but never a `session_start` is now silently reported as healthy-but-new. Reachable, not synthetic: `hooksDeclarePlugin` queries `SessionStart` only, so a hooks block registering `PostToolUse`/`Stop` but not `SessionStart` (measured: reads `declared nowhere`) produces exactly this file — journal lines written, no anchor — and the command then says `no run file in aidd_docs/runs` about a file that exists | Branch on `journals.length` before `sessionJournalsOf` filtering: a run file that exists with no readable anchor is its own reason (a torn or anchorless run file), never `no run file … yet`. Extend `telemetry-check.e2e.test.ts:554` to seed an anchorless journal with the recorder declared | fixed — `noRunFileClaim` branches on the raw (unfiltered) journal count before the declared/undeclared split: any run file present with no readable session anchor grades its own `anchorless-run-file` reason at `fail`, unconditional on the recorder's declaration, never `no run file … yet`. Verified on the built CLI, sandboxed HOME, before/after: before, recorder-declared + anchorless file read `hook fired --  no run file … nothing to evaluate`; after, both declared and undeclared read `hook fired FAIL  1 run file(s) …, but none carry a readable session_start …`. `telemetry-check.e2e.test.ts` extended with the anchorless+declared case; the pre-existing torn-file (undeclared) e2e's assertion corrected from the old, now-false wording to the new one |
| 🔴 | fit | 1 | `cli/src/infrastructure/adapters/telemetry-evidence-adapter.ts:140-143` | `invokesScript` matches the bare leaf `journal.cjs`. Measured: `.cursor/hooks.json` registering `node ./.cursor/hooks/some-other-plugin/journal.cjs session-start` prints `recorder declared  yes` and flips `hook fired` from `FAIL` to `--`. This directly violates the spec's own discriminator — "the two are told apart by reading whether the recorder is declared" — since matching any plugin's `journal.cjs` is not reading whether **this** recorder is declared, and it fails in the masking direction. The repo already solved plugin-unique identification for this exact file: `stripPluginEntries` (`cursor-hooks-project-merge.ts:72-83`) matches on `cursorProjectHooksScriptDir(pluginName)` = `.cursor/hooks/aidd-telemetry/` | Match the plugin-unique path marker, not the leaf: reuse `cursorProjectHooksScriptDir(PLUGIN_NAME)` for the project-hooks route and the plugin-root token for the Claude route | fixed — `invokesScript`'s leaf-boundary regex replaced with `invokesRecorderEntryPoint`, matching the plugin-unique path markers this build actually writes: the unexpanded `${CLAUDE_PLUGIN_ROOT}/hooks/journal.cjs` token, the `--flat`-rewritten `.claude/hooks/aidd-telemetry/journal.cjs` (derived from the same `genericFlatHooksScriptPath` primitive the flat build itself uses), and `cursorProjectHooksScriptDir(PLUGIN_NAME)`'s `.cursor/hooks/aidd-telemetry/journal.cjs`. Verified on the built CLI, sandboxed HOME, before/after: before, `node /autre-plugin/hooks/journal.cjs session-start` read `recorder declared yes` and flipped `hook fired` to `--`; after, `recorder declared nowhere this build checks`. e2e: "never reads another plugin's own journal.cjs as this recorder being declared" |
| 🟡 | functional | 3 | `plugins/aidd-telemetry/skills/02-check/actions/02-diagnose.md:49-52` | The skill's account of the Codex-trust verdict is false: a `config.toml` that exists with no trust table reads `untrusted-codex-hook`, not the declared/nowhere reading the prose promises. The clause pre-dates this diff (`git show 05d346f1` reads "falls back to never-fired") but commit ae6dde28 rewrote exactly this sentence into the new vocabulary under phase 3 task 1, carrying the error forward instead of correcting it. The phrase-presence guard cannot see it | Restate the condition the code actually takes: an **absent** `config.toml` falls back to the declared/nowhere reading; one that exists and approves nothing reads untrusted. Align the prose with the Test table row two lines below it | fixed — step 8 restated: only an **absent** `config.toml` falls back to the declared/nowhere reading; one that exists and approves nothing reads untrusted, aligned with the Test table row beneath it. No code change needed — `hook-trust-reader-adapter.ts` already took this path (`readable: false` only on ENOENT); the prose was the only thing wrong |
| 🟡 | functional | 3 | `cli/tests/domain/models/telemetry-claim.unit.test.ts:420-457` | The drift guard is three literal phrases checked for presence anywhere in the file, so it does not bite on a verdict disagreement — proven by mutation (inverting the verdict `02-diagnose.md:42` teaches leaves 33/33 green). It also carries no marker for any verdict added later, and resolves the skill path off `process.cwd()` + `".."`, coupling it to being run from `cli/` | Assert the phrase inside the sentence that names the verdict (e.g. match a regex tying `` `--` `` to "nothing to evaluate" and `` `FAIL` `` to "declared nowhere" in the skill), and derive the phrase set from the reason ids so a new reason without an account fails | fixed — the guard scopes every check to step 6 alone (isolated by its own stable header, not the whole file), ties each verdict token to its phrase in the same clause, and is keyed by `NoRunFileReason` — the exhaustive union `noRunFileClaim`'s own branches compose into — so a fifth branch without an account is a compile error. Proved by mutation during validation: inverting `02-diagnose.md`'s own declared/not-yet-fired pairing (the reviewer's own repro) turned it red; reverted, tree clean. Also proved on the code side: renaming the `nothing to evaluate` detail string turned it red too; reverted, tree clean |
| 🟡 | fit | 1 | `cli/src/infrastructure/adapters/telemetry-evidence-adapter.ts:191` | `claudeSettingsCandidates` is reused for the declaration read, but its own comment (`:39-43`) justifies the Claude-only set for **export leftovers** ("Only Claude Code ever wrote a real settings-file export") — a justification that does not transfer. Copilot declares `enabledPlugins` in `.github/copilot/settings.json` (`domain/tools/ai/copilot.ts:341-343`), which is never read. Measured: that file declaring `aidd-telemetry@aidd` reads `nowhere this build checks`. The manifest route masks it in the aidd-driven flow, but `TelemetryRecorderDeclarationSetup`'s "a tool's own settings file declaring its enabled plugins" covers 1 of the 2 tools that have one | Resolve the enabled-plugins settings path from each tool's own `marketplaceSettings.enabledPluginsSettingsPath ?? settingsPath` (the same resolution `marketplace-sync-settings-use-case.ts:304` writes through), rather than reusing a set justified for another read | fixed — new `enabledPluginsCandidates()` resolves each AI tool's own `marketplaceSettings.enabledPluginsSettingsPath ?? settingsPath` from the tool registry, the same resolution `marketplace-sync-settings-use-case.ts:304` writes through, replacing the reused export-leftover list for this one read. Picks up Copilot's `.github/copilot/settings.json`; `claudeSettingsCandidates` keeps its own comment narrowed to the hooks-block route and the export-leftover read, both genuinely justified for it. Verified on the built CLI: `.github/copilot/settings.json` declaring `aidd-telemetry@ai-driven-dev/framework` now reads `recorder declared yes`. e2e: "names Copilot's own settings file as a declaration route…" |
| 🟡 | rot | 2 | `cli/src/domain/models/telemetry-claim.ts:161-163` | The `--` detail cites `claude-cli-adapter.ts` unconditionally. Measured with a Cursor-only declaration (`recorder declared yes — .cursor/hooks.json`), the caveat still names a Claude-specific headless-plugin-registration case that cannot apply. Under-promising, so not a false promise, but the citation is incoherent with the route named one line above. Related: `declared` is OR'd across every tool, so `recorder declared yes` can be true for Claude while the person runs Cursor | Make the caveat route-aware, or drop the file citation and state the general form ("a declaration can be dropped without the recorder ever registering"), keeping the measured case in the source comment where it belongs | not actioned — not among the 7 items this pass was dispatched to fix; the `--` detail still cites `claude-cli-adapter.ts` unconditionally |
| 🟡 | conform | 1 | `cli/src/infrastructure/adapters/telemetry-evidence-adapter.ts:136-143` | `invokesScript` encodes a domain rule — what counts as this recorder's entry point — in the adapter layer, which `cli/.claude/rules/00-architecture/0-layer-responsibilities.md` restricts to "I/O and format translation only". The sibling shape knowledge was correctly pushed into `domain/formats/flat-hooks-merge.ts`; the recognition rule stayed behind | Move the entry-point predicate into the domain beside `HOOK_ENTRY_SCRIPT` (or into `flat-hooks-merge.ts` next to `hookCommandsForEvent`), leaving the adapter with the reads | not actioned — not among the 7 items this pass was dispatched to fix; the rewritten entry-point predicate (see the critical `fit`/1 fix above) still lives in the adapter, not the domain |
| 🟡 | code | 1 | `cli/src/infrastructure/adapters/telemetry-evidence-adapter.ts:142` | The regex requires the leaf be followed by whitespace or end-of-string, so a quoted command (`node "…/journal.cjs" session-start`) is a false negative — measured `nowhere this build checks`. `npx`/wrapper invocations miss too. No route this repo writes ever quotes, so the exposure is hand-written hooks blocks, which are exactly the case commit a048690e was added to cover | Accept a closing quote as a terminator, or match the plugin-unique directory marker (which subsumes both) per the critical finding above | fixed as a consequence of the critical `fit`/1 fix above: the new marker match is a plain substring check against a multi-segment, plugin-unique path, so a quoted command (`node "…/journal.cjs" session-start`) matches with no separate boundary logic. Verified on the built CLI, before/after: before, a quoted `${CLAUDE_PLUGIN_ROOT}/hooks/journal.cjs` command read `recorder declared nowhere this build checks`; after, `recorder declared yes`. e2e: "still recognises the recorder's own hooks-block entry point when the command is quoted" |
| 🟢 | rot | 1 | `cli/src/domain/ports/telemetry-evidence-reader.ts:29-36,60-62` | The interface doc still says the port covers "whether the project switch is on, the unrecognised-payload marker, and … a stale export configuration" — the two methods this diff added are absent from it; `readRecorderDeclaration`'s own doc says "the AIDD manifest and a tool's own settings file", omitting the hooks-block route commit a048690e added | Add the two reads to the interface doc and name the hooks-block route in `readRecorderDeclaration`'s | not actioned — not among the 7 items this pass was dispatched to fix; `telemetry-evidence-reader.ts`'s interface doc is unchanged |
| 🟢 | rot | 1 | `cli/tests/helpers/ports/stub-telemetry-evidence-reader.ts:16-25` | The stub defaults contradict each other: `enabled = true` (`isTelemetryEnabled`) beside `switchSetup.enabled = false`. Every test on this stub renders `measurement allowed  no — /repo/.aidd/config.json` while the claims still run past the gate | Default `switchSetup.enabled` to `true`, or derive it from the `enabled` field so one fact cannot disagree with itself | not actioned on the described contradiction — not among the 7 items this pass was dispatched to fix; `switchSetup.enabled` stays `false` beside `enabled = true`. Mechanically touched only: `recorderDeclaration` gained `unreadable: []` to satisfy the new required field, unrelated to this finding's complaint |
| 🟢 | code | 1 | `cli/src/infrastructure/adapters/telemetry-evidence-adapter.ts:100-134` | `manifestDeclaresPlugin` and `settingsDeclaresPlugin` repeat the same 8-line `readIfExists` → `JSON.parse` in `try/catch` → `asPlainObject` preamble. `cli/.claude/rules/07-quality/7-clean-code.md` requires a helper at ≥2 callers | Extract `readJsonIfExists(path): Promise<unknown \| null>`; it is also the natural seam for the `readable` distinction the critical finding needs | fixed — `readJsonIfExists(path)` extracted, returning `{status: "absent"\|"unreadable"\|"ok", raw, value}`; `manifestDeclaresPlugin`, `settingsDeclaresPlugin`, and `hooksDeclarePlugin` all read through it, and it is exactly the seam finding 1 needed for the ENOENT-vs-damaged distinction |
| 🟢 | code | 1 | `cli/src/application/display/telemetry-check-display.ts:62` | `locationsChecked.join(", ")` emits all five absolute paths on one unwrapped line (measured ~500 chars). The row exists so a person can go and add the declaration; a single wrapped line is the hardest form to act on | Print the checked locations one per indented line under the row, or name the two a person would realistically edit | fixed — `locationsChecked` now prints one indented path per line under the `recorder declared` row instead of a single ~500-char comma-joined line. Verified on the built CLI: the row reads `nowhere this build checks — looked in:` followed by five indented lines |
| 🟢 | code | 1 | `cli/src/infrastructure/adapters/telemetry-evidence-adapter.ts:198-205` | Up to seven file reads run sequentially in two `for … await` loops, while the caller (`diagnose-telemetry-use-case.ts:120-123`) deliberately parallelises its two reads | `Promise.all` over the candidates, then order `declaredAt` by the candidate list | not actioned — not among the 7 items this pass was dispatched to fix; the manifest/enabledPlugins/hooks reads stay sequential (the hooks loop's `declaredAt.includes(path)` short-circuit needs the manifest and enabledPlugins results settled first, so a straight `Promise.all` would need restructuring beyond this pass's scope) |

## Verification

| Metric        | Value                                             |
| ------------- | ------------------------------------------------- |
| Verified      | 75% (12/16)                                       |
| Files checked | `cli/src/domain/models/telemetry-setup.ts`, `cli/src/domain/models/telemetry-claim.ts`, `cli/src/domain/ports/telemetry-evidence-reader.ts`, `cli/src/domain/formats/flat-hooks-merge.ts`, `cli/src/infrastructure/adapters/telemetry-evidence-adapter.ts`, `cli/src/infrastructure/adapters/hook-trust-reader-adapter.ts`, `cli/src/infrastructure/deps.ts`, `cli/src/application/use-cases/telemetry/diagnose-telemetry-use-case.ts`, `cli/src/application/display/telemetry-check-display.ts`, `cli/tests/domain/models/telemetry-claim.unit.test.ts`, `cli/tests/domain/models/telemetry-setup.unit.test.ts`, `cli/tests/domain/formats/flat-hooks-merge.unit.test.ts`, `cli/tests/e2e/telemetry-check.e2e.test.ts`, `cli/tests/helpers/ports/stub-telemetry-evidence-reader.ts`, `plugins/aidd-telemetry/skills/02-check/SKILL.md`, `plugins/aidd-telemetry/skills/02-check/actions/02-diagnose.md`, `plugins/aidd-telemetry/CATALOG.md`, `docs/prompts-documentation.md` |
| Unchecked     | Phase 1 "a location that cannot be read says so, and every other stated fact still appears" — fix; Phase 2 "every case that already had a run file keeps its previous verdict" — fix; Phase 3 "the skill's account of each verdict matches what the command prints" — fix; Phase 3 "a guard fails when the skill and the command disagree about a verdict" — fix |
| Unplanned     | `hookCommandsForEvent` + `collectCommands` added to `cli/src/domain/formats/flat-hooks-merge.ts:295-324`, a module the phase-1 architecture projection does not list — placement is sound (it prevents a second copy of the four hook shapes in the adapter) and is covered by `flat-hooks-merge.unit.test.ts`; `hook-trust-reader-adapter.ts:24` newly exports `HOOK_ENTRY_SCRIPT`, also unlisted and correct (one literal, not two) |
