# Review: the identity file is the person

- **Verdict**: changes-requested
- **Diff**: `eb7a0a49...b7c28615`
- **Axes run**: code, functional, relevancy (plus justification-verification, run as a first-class pass over every `Decisions` row and every added doc-comment "because")
- **Date**: 2026_08_28
- **Findings**: 1 critical, 6 warning, 8 minor

## Phases

### Phase 1 — The identity carries who is also this person

- [x] An identity can be built with no display name and no added identifiers, and reads back with neither invented — `cli/src/domain/ports/person-identity-reader.ts:26-31` (`alsoMe` required, `displayName?` optional), test `cli/tests/domain/models/person-resolution.unit.test.ts:107`
- [x] The person's own identifier and any added one resolve to the same person — `cli/src/domain/models/person-resolution.ts:27-29` (`matches`), tests `person-resolution.unit.test.ts:20` and `:28`; mutation-verified (dropping the `identity.personId === rawId` half of `matches` fails 1 test: `2777 passed | 1 failed`)
- [x] No identifier resolves to `none`, an undeclared one to `unresolved`, and the two remain distinguishable — `person-resolution.ts:48-52`, tests `:35`, `:43`, `:50`, `:57`; mutation-verified (returning `unresolved` for the empty-`rawId` branch fails 6 tests across 3 files)
- [x] A `null` identity resolves a real identifier to `unresolved` with that identifier still named — `person-resolution.ts:49-51`, tests `:68`, `:75`
- [x] Nothing in the codebase can express two people claiming one identifier — `person-resolution.ts:38-42` (the type is the guard, stated), `person-identity-reader.ts:6-9`, compile-time test `person-resolution.unit.test.ts:113-130`; double-mutation-verified: removing the directive yields exactly `tests/domain/models/person-resolution.unit.test.ts(125,7): error TS2353: ... 'entries' does not exist in type 'PersonIdentity'` and nothing else, and adding a roster field back to `PersonIdentity` yields `error TS2578: Unused '@ts-expect-error' directive`. `cli/tsconfig.json:24` includes `tests/**/*`, and `lefthook.yml:124-135` runs `cd cli && pnpm typecheck`, so the assertion is in the enforced program
- [ ] Every case the deleted test file carried still exists, and still fails when the behaviour it guards is broken — `person-mapping.unit.test.ts:121` ("an identity written twice inside one person resolves without a refusal") has no analogue in `person-resolution.unit.test.ts`. Its ported subject is `resolvePerson`'s `identity.alsoMe.includes(identity.personId)` branch (`person-resolution.ts:56-58`); the new fixture's `alsoMe` never contains `personId`, so the branch is uncovered. Mutation-verified dead: collapsing the ternary to `[identity.personId, ...identity.alsoMe]` leaves `251 files / 2778 tests` all passing. See finding 5

### Phase 2 — One store, one file, one set of verbs

- [x] One file holds the identifier, how it was obtained, and every added identifier — `cli/src/infrastructure/adapters/person-identity-adapter.ts:55-68` (`serializeIdentity`), `:36-51` (`parseIdentity`); confirmed on the built CLI under a sandboxed `HOME`: `{"person_id":"person-shared","origin":"adopted","also_me":["a-1","b-2"]}`, mode `0600`
- [x] An identity file written before this change reads back as created here, with nothing invented — `person-identity-adapter.ts:32-42` (`parsed.origin === "adopted" ? "adopted" : "minted"`), e2e `cli/tests/e2e/telemetry-identity.e2e.test.ts:390-400`
- [x] The declaration is read from the OS profile even when a project-scoped config directory is set — `person-identity-adapter.ts:21-23` + `cli/src/infrastructure/home-dir.ts:40`, e2e `cli/tests/e2e/telemetry-identity-resolution.e2e.test.ts:263-296`; independently mutation-proven by the caller (honouring `AIDD_USER_CONFIG_DIR` in `resolveAiddConfigDir` fails that e2e with `expected [ 'person-a', 'machine-b-id' ] to not include 'machine-b-id'`)
- [x] Taking an identifier from another machine leaves one person, obtained as taken — `person-identity-use-case.ts:114-125`, `person-identity-adapter.ts:103-112`; built CLI: `AIDD identity: now person-shared (replacing 289d2611-…)` then `status` → `person-shared (taken from another machine)`, file `"origin": "adopted"`
- [x] Taking the identifier already in effect writes nothing and says it is already in effect — `person-identity-use-case.ts:115-118` (early return before `adopt`), display `telemetry-display.ts:150-155`; built CLI: message `is already in effect`, and the file's sha1 **and** mtime are byte-identical across the call (`1c22fd46… / 1787909445.331483945` before and after)
- [x] Adding an identifier before there is a person is refused, naming the missing step — `person-identity-use-case.ts:128-130` (`IdentityRequiredToLinkError`), test `person-identity-use-case.unit.test.ts:180`; built CLI: `Error: No identity to link onto yet. Run \`aidd telemetry identity on\` first.` exit 1
- [x] Asking who this machine's user is shows the identifier, its origin, and every added identifier — `telemetry-display.ts:111-138`, tests `:59`, `:72`, `:85`; built CLI: `on, person-shared (taken from another machine) (…)` + `Identifiers added onto this person: a-1, b-2`
- [x] A declaration that cannot be parsed is refused by name, never reported as nobody — `person-identity-adapter.ts:87-94` (`readStrict`), `person-identity-use-case.ts:95-103`; built CLI on a corrupt file: `Error: Could not read the identity file at … (Unexpected token 'o', "not json at all" is not valid JSON).` exit 1, for `status`, `use` and `link` alike
- [ ] Withdrawing leaves no declaration, and states how many added identifiers went with it — holds for every file that parses (built CLI: `3 added identifiers removed with it.`, directory emptied), but not for one this diff's own new verb can write: `identity use ""` writes `{"person_id":"","origin":"adopted"}`, which `parseIdentity` rejects at `person-identity-adapter.ts:38`, so `off` takes the `existing === null && !discardedDamaged` branch at `person-identity-use-case.ts:157-159`, answers `already off - nothing to withdraw`, and **leaves the file on disk**. See finding 2
- [x] Withdrawing still works on a declaration too damaged to parse — `person-identity-use-case.ts:145-176` (`readForWithdrawal`), `person-identity-adapter.ts:136-143` (`rm` with `recursive`/`force`), e2e `telemetry-identity.e2e.test.ts:217-231` (seeds `identity.json` as a **directory**, so the `recursive: true` justification is backed by a real case); built CLI: `off` on corrupt content → `The identity file could not be read, so it was discarded rather than left behind.` exit 0, file gone

### Phase 3 — The report reads it, and names what stopped it

- [x] A report resolves people against the identity file, with no second file present anywhere — `report-cost-use-case.ts:137`, `deps.ts:797`, `cost-report.ts:831`; every mapping file, port, adapter, use case, double and test is deleted (10/10 confirmed absent at HEAD)
- [x] The person rows, their identities and their totals are unchanged from the previous delivery — `cost-report.ts:494-519` untouched by this diff, e2e `telemetry-identity-resolution.e2e.test.ts:166-190` keeps the reconciliation and never-merge assertions verbatim; mutation-verified (collapsing the `unresolved` group key to `NO_KNOWN_PERSON` fails 5 tests)
- [x] A declaration that cannot be read and no declaration at all produce different caveats — `cost-report-artefact.ts:136-143`, e2e `telemetry-identity-resolution.e2e.test.ts:236-261`, unit `cost-report-artefact.unit.test.ts:137-150`
- [x] An unrecognised error is not reported as either named cause — `report-cost-use-case.ts:103-108` (`instanceof UnreadableIdentityFileError`, else re-throw), test `report-cost-use-case.unit.test.ts:179-185`
- [x] Every figure is still reported whichever cause fired — `report-cost-use-case.ts:97-109` never aborts, tests `report-cost-use-case.unit.test.ts:161`, `:172`, e2e `:204`, `:222`
- [x] The two-machines journey passes with the identity taken through the CLI, not by writing a file — `telemetry-identity-resolution.e2e.test.ts:60-64` (`useIdentity` drives `identity use`), `:122-164`
- [x] The project-scoped journey asserts a non-empty report before asserting no row resolved — `telemetry-identity-resolution.e2e.test.ts:288` (`expect(envelope.totals.requests).toBe(2)`) before `:292-295`
- [x] No documentation mentions the separate declaration file — `grep` for `person_mapping_unusable|person-mapping|PersonMappingStore|validatePersonMapping|AmbiguousPersonMapping` over the whole tree returns only three deliberate historical notes in doc comments (`json-file.ts:4`, `person-identity-adapter.ts:19`, `errors.ts:515`) and one in the contract's versioning history (`cost-report-contract.md:118`); the `status` line naming a stale file is phase 2 task 3.3's own requirement
- [x] The generated catalogue and prompt index match what their scripts produce — re-ran both generators from `lefthook.yml:61-88` in place; `git status --porcelain` stayed empty for `plugins/aidd-telemetry/CATALOG.md` and `docs/prompts-documentation.md`

## Findings

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| 🔴 | rot | - | `aidd_docs/tasks/2026_08/2026_08_28_identity-is-the-person/plan.md` — `Decisions` row "An identity file with no origin reads as `minted`; the separate declaration file gets no migration at all" | The justification is false against the repository, and it is the same defect class this rework exists to eliminate. It asserts "`identity.json` **shipped before this branch**, written by the plain-node script `38de97d8` deleted, so **files without `origin` exist in the world**". Neither half holds. `6c2b2a56` (2026-08-23, "a person can choose to be named, and take it back") is the first commit that ever touched `identity.json` and is an ancestor of `HEAD` **on this same branch**; `git tag --contains 6c2b2a56` is empty; `git merge-base --is-ancestor 6c2b2a56 main` → NO; `git ls-tree -r main --name-only \| grep -i identity` is empty and `git show main:cli/src/infrastructure/adapters/person-identity-adapter.ts` → "exists on disk, but not in 'main'". `identity.json` has never been released, so no file without `origin` exists anywhere but a developer's own working tree. The asymmetry the row defends — compatibility for one unreleased file, no migration for another unreleased file — is therefore stated on a fact that is not true, exactly as `person-mapping-adapter.ts`'s "two independent writers agree on byte for byte" was. The compat itself is harmless and worth keeping; only its stated reason is wrong. | Rewrite the row to the true asymmetry: neither file shipped, but `identity.json` already exists on the machines of everyone working this branch and is written by a shape this adapter itself produced, so defaulting a missing `origin` costs one line and loses nothing, while `person-mapping.json` needs no code because nothing reads it. State that "in the world" means this branch's own working machines, not released users. |
| 🟡 | code | 2 | `cli/src/application/commands/telemetry.ts:312-325`; `cli/src/application/use-cases/telemetry/person-identity-use-case.ts:114-125`; `cli/src/infrastructure/adapters/person-identity-adapter.ts:38` | `identity use` accepts any string with no validation. `use ""` reports success (`AIDD identity: now  (…)`), writes `{"person_id":"","origin":"adopted"}` at `0600`, and that file then reads back as **no identity** (`parseIdentity` rejects an empty `person_id`). `status` answers `off - records carry no person` while the file exists, and `off` answers `already off - nothing to withdraw` and **does not remove it** — reproduced on the built CLI under a sandboxed `HOME`, file still present after `off`. This breaks the spec's "Withdrawing removes the whole declaration" and phase 2's own rule, written at `cli/tests/e2e/telemetry-identity.e2e.test.ts:216-218`: "there must be no state a damaged file can put someone in that withdrawing cannot get them out of." `use "  "` is likewise accepted and becomes the person id; `link ""` appends an unmatchable empty entry to `also_me` and inflates `off`'s count. `name` already refuses empty and whitespace-only input via `EmptyDisplayNameError` (`person-identity-use-case.ts:167`), so the omission is inconsistent inside one use case. | Refuse an empty or whitespace-only identifier in `use` and `link` the way `name` already does, reusing one guard. Independently, make `off` remove a file that exists but reads back as `null`, so no writable state escapes withdrawal. |
| 🟡 | fit | 3 | `cli/src/application/display/cost-report-artefact.ts:140-142`, spliced in at `:158`, `:168` and `:231` | `identity_unusable: "absent"` fires whenever no identity is declared — the default state of every user who has never run `identity on` — and `caveats()` is spliced into **every** artefact, not the person one. Reproduced on the built CLI with an empty sandboxed profile: `telemetry report --axis total` prints `nothing in this period` followed by `no identity was declared; every identifier is reported unresolved`, and `--axis model` prints it under an empty model table. At `eb7a0a49` the field was `false` in this state and no caveat printed; the caveat was reserved for a genuinely damaged file. The envelope reshape is planned (phase 3 task 2.2) and right; this collateral is not: phase 3's Test Scope frames both caveats under "report by person", every unit test for them passes axis `"person"` (`cost-report-artefact.unit.test.ts:113`, `:126`, `:139-144`), and no test covers any other axis. The result is a permanent notice about a resolution nobody asked for, on reports with no person column and often no rows at all. | Emit the `absent` caveat only on the person axis (or only when at least one record carried an identifier), leaving `unreadable` on every axis where it still signals real damage. Add a test pinning that `--axis total` on a profile with no identity prints no person caveat. |
| 🟡 | rot | 3 | `aidd_docs/product/cost-report-contract.md:323-327` | "**Any of the three present or non-zero means your total is partial.** Say so rather than presenting it as whole." — with `identity_unusable` now present in the ordinary no-identity state, this instructs every consumer to declare every default-state report partial, and the very next sentence contradicts it: "Either way every record is still counted … never as a reason to drop a figure." A skill following this literally will caveat a complete total. | Scope the "partial" rule to `undated_records` and `unreadable_lines`, and state that `identity_unusable` changes how records are labelled, never how many are counted — the wording `by_person`'s own section (`:206`) already uses. |
| 🟡 | functional | 1 | `cli/tests/domain/models/person-resolution.unit.test.ts` | Phase 1 acceptance criterion "Every case the deleted test file carried still exists, and still fails when the behaviour it guards is broken" is unmet. `person-mapping.unit.test.ts:121` is the one case with no analogue, and the `resolvePerson` branch that inherited its subject (`person-resolution.ts:56-58`) is mutation-proven uncovered — collapsing the ternary leaves the whole suite green (`251 files / 2778 tests` passing). The phase's own task 4.2 named this as "the failure this phase most risks". | Either add the ported case — an identity whose `alsoMe` already contains its `personId` resolves to that person once, with no duplicate in `identities` — or delete the branch as unreachable and let `link` refuse an identifier equal to `personId` (see the next row), so no untested path remains. |
| 🟡 | functional | 2 | `cli/src/application/use-cases/telemetry/person-identity-use-case.ts:145-176` | Phase 2 acceptance criterion "Withdrawing leaves no declaration, and states how many added identifiers went with it" is unmet for a file this diff's own `use` verb can produce. Detailed in the `identity use ""` row above. | As above: validate the identifier, and make `off` remove a present-but-unreadable-as-`null` file. |
| 🟡 | code | 2 | `cli/src/application/use-cases/telemetry/person-identity-use-case.ts:61-66` and `:130` | `PersonIdentityLinkResult.alreadyListed` is documented as "`true` when `identity` already resolved to this same person before this call", but the check is `person.alsoMe.includes(identity)` alone. The person's own `personId` already resolves to them and is not caught: reproduced on the built CLI, `identity link person-shared` on the identity `person-shared` reports a fresh link and appends `"person-shared"` into its own `also_me`. `off` then counts it among the "added identifiers removed", and it is the only way to reach the uncovered branch in the row above. | Treat `identity === person.personId` as already listed, so the doc comment and the code state the same rule and the redundant self-entry cannot be written. |
| 🟢 | conform | 2 | commit `66486466` | Landed knowingly red: its own message states "The e2e journey … is expected red until phase 3 rewrites it against `identity use`/`identity link`." Nothing was skipped — the phase-1 deletions it deferred (`person-mapping.ts`, `AmbiguousPersonMappingError`, `validatePersonMapping`) are all absent at `HEAD`, and the commit messages disclose every forward/backward move — but the sequence is not bisectable through this commit. | Note it, or squash phases 2 and 3 in future reworks where a subsystem deletion cannot be green on its own. |
| 🟢 | rot | 1 | `aidd_docs/tasks/.../phase-1.md:11-16` | The architecture projection marks `models/person-mapping.ts ❌` and `person-mapping.unit.test.ts ❌` as phase-1 deletions and the file is `status: done`, but both deletions happened in phase 2 (`66486466`). The commit message is honest about it; the phase doc is not. | Move the two `❌` entries to phase-2's projection, or note the deferral in phase-1.md. |
| 🟢 | rot | 3 | `cli/tests/application/display/cost-report-artefact.unit.test.ts:48-50`; `cli/tests/domain/models/cost-report-person.unit.test.ts:205`, `:207-212` | Mapping vocabulary survives the type change in the tests this diff rewrote: `function onePersonMapping(): PersonIdentity`, `describe("byPeople — no mapping declared at all")`, and the locals `withMapping`/`withoutMapping` — in a diff whose whole point is that a mapping no longer exists. (The substitution itself is clean: every case in `cost-report-person.unit.test.ts` survives, only the subject's construction changed, which is the evidence for phase 3 criterion 2.) | Rename the helper to `thisMachinesIdentity()`, the describe to "no identity declared at all", and the two locals to `withIdentity`/`withoutIdentity`. |
| 🟢 | rot | 3 | `cli/tests/e2e/telemetry-identity.e2e.test.ts:388` | The describe block is still named "the on-disk format the deleted script produced", but this diff explicitly ends that parity — its own updated comment says "byte parity with the deleted script's own output ends here by design". The block's name now asserts the opposite of what it tests. | Rename to what it now pins: the on-disk shape the adapter writes, and that a file without `origin` still reads back. |
| 🟢 | rot | 2 | `cli/src/infrastructure/adapters/person-identity-adapter.ts:16-20`; `cli/src/infrastructure/json-file.ts:1-8` | Two abstractions are kept alive with speculative justifications, in a diff whose own plan condemns exactly that ("Reserving an enum value for a mechanism that does not exist is the speculative shape this work has already paid for twice"). `resolveAiddConfigDir` is "kept as its own function since a second reader of this directory is exactly what the identity-is-the-person rework does not rule out" — it now has one consumer file. `json-file.ts` is "kept as its own module … since the three questions it answers … are not specific to the one adapter that asks them today" — `grep` for `json-file.js` returns exactly one importer. Neither is wrong to keep; both reasons are "something might need it later". | Justify each by what it does today — `resolveAiddConfigDir` isolates the deliberate refusal of `AIDD_USER_CONFIG_DIR` beside the other home-dir resolvers, `json-file.ts` keeps raw-JSON narrowing out of an adapter — and drop the speculative clause. |
| 🟢 | code | 2 | `cli/src/application/display/telemetry-display.ts:196-198` | `off` on a file too damaged to parse prints `0 added identifiers removed with it.` The result type is honest about this (`addedIdentifiersRemoved`'s doc: "`0` both when none were added and when a damaged file meant this call never learned how many there were"), but the line asserts a count it does not know, right after saying the file could not be read. | Skip the count line when `discardedDamaged` is true, or say "how many identifiers went with it could not be read". |
| 🟢 | conform | 1 | `cli/tests/domain/models/person-resolution.unit.test.ts:124`; `cli/.claude/rules/00-architecture/0-hexagonal.md` ("Type honesty") | The rule reads "No type is widened through `as unknown as`, `as any`, `as never`, `@ts-expect-error` or `@ts-ignore`, in `tests/` as much as in `src/` — `scripts/check-cli-layering.mjs` enforces it over both trees". This diff adds the repository's directive-based compile-time assertion without noting the tension. Two separate points: the use here does not widen a type (it asserts an error exists, which is the opposite), and the rule's own claim about the enforcement script is false — `scripts/check-cli-layering.mjs:26-28,100` only matches `as unknown as` / `as never`, never the two directives. The script passes: `Dependencies point inward, and no type is widened through unknown or never.` | Scope the rule's sentence to type-widening casts and name the one legitimate directive use (a compile-time refusal assertion), or add the file to a `DIRECTIVES_ALLOWED` list so the rule and the script agree. |
| 🟢 | rot | - | `aidd_docs/tasks/.../plan.md` — `Resources` row 1 | "`38de97d8` deleted the plain-node identity writer" is true but incomplete: `git log --all --diff-filter=D --name-only -- '*identity.cjs'` shows `38de97d8` deleted `skills/00-init/scripts/lib/identity.cjs` **and** `skills/00-init/scripts/telemetry-identity.cjs`, while a second copy at `skills/01-cost/scripts/lib/identity.cjs` was deleted by `40f13dcc`. The "CLI is the sole writer today" conclusion holds — `git grep identity.json` over `plugins/` returns only prose in `04-identify.md`. | Name both deletions, so the evidence matches the claim it supports. |
| 🟢 | rot | 3 | `cli/tests/application/display/cost-report-artefact.unit.test.ts:64-74` | Carried-over debt the previous review raised and left skipped: the test named "prints one row per person with the identities behind it, **mapped rows first**" builds an envelope with exactly one row, so the ordering half of its name is never exercised. This diff edited that exact test (`personMapping:` → `identity:`) without closing it. | Add an unresolved record and assert the mapped label appears above it, or drop "mapped rows first" from the name. |

## Verification

| Metric | Value |
| ------ | ----- |
| Verified | 92% (23/25) |
| Files checked | `cli/src/domain/models/person-resolution.ts`, `cli/src/domain/models/cost-report.ts`, `cli/src/domain/models/cost-report-envelope.ts`, `cli/src/domain/errors.ts`, `cli/src/domain/ports/person-identity-reader.ts`, `cli/src/domain/ports/person-identity-store.ts`, `cli/src/infrastructure/adapters/person-identity-adapter.ts`, `cli/src/infrastructure/deps.ts`, `cli/src/infrastructure/errors.ts`, `cli/src/infrastructure/json-file.ts`, `cli/src/infrastructure/home-dir.ts`, `cli/src/application/use-cases/telemetry/person-identity-use-case.ts`, `cli/src/application/use-cases/telemetry/report-cost-use-case.ts`, `cli/src/application/display/telemetry-display.ts`, `cli/src/application/display/cost-report-artefact.ts`, `cli/src/application/commands/telemetry.ts`, `cli/tests/domain/models/person-resolution.unit.test.ts`, `cli/tests/domain/models/cost-report-person.unit.test.ts`, `cli/tests/domain/models/cost-report-envelope.unit.test.ts`, `cli/tests/application/use-cases/telemetry/person-identity-use-case.unit.test.ts`, `cli/tests/application/use-cases/telemetry/report-cost-use-case.unit.test.ts`, `cli/tests/application/use-cases/telemetry/read-local-cost-use-case.unit.test.ts`, `cli/tests/application/display/cost-report-artefact.unit.test.ts`, `cli/tests/helpers/ports/in-memory-person-identity-store.ts`, `cli/tests/e2e/telemetry-identity-resolution.e2e.test.ts`, `cli/tests/e2e/telemetry-identity.e2e.test.ts`, `cli/tests/e2e/helpers.ts`, `cli/tests/fixtures/cli-owns-read/expected-envelope.json`, `aidd_docs/product/cost-report-contract.md`, `plugins/aidd-telemetry/README.md`, `plugins/aidd-telemetry/skills/00-init/actions/04-identify.md`, `plugins/aidd-telemetry/skills/01-cost/actions/03-report.md`, `plugins/aidd-telemetry/CATALOG.md`, `docs/prompts-documentation.md`, `scripts/check-cli-layering.mjs`, `lefthook.yml`, plus the 10 deleted mapping-subsystem files confirmed absent |
| Unchecked | Phase 1 "Every case the deleted test file carried still exists, and still fails when the behaviour it guards is broken" — fix. Phase 2 "Withdrawing leaves no declaration, and states how many added identifiers went with it" — fix (holds for every parseable file; fails for one `identity use ""` can write) |
| Unplanned | `withAlsoMeAdded`/`withAlsoMeRemoved` in `person-resolution.ts:64-77`, asked for by no phase — justified: it closes the previous review's skipped finding 5 (the in-memory double reimplementing the adapter's merge rule line for line); the double and the adapter now share one source. `staleMappingFilePath()` on `PersonIdentityStore`, asked for by phase 2 task 3.3's "say it is ignored and can be removed" but not by a criterion — verified working on the built CLI. `identityInputFields` in `report-cost-use-case.ts:113-123`, asked for by no phase — a 9-line shape assembler for a 2-field object, justified by analogy to `cost-report.ts`'s `readFields`; borderline, not a finding. Bundle budget untouched at 590 KB while the measured bundle fell 567.7 → 565.2 KB (`npx tsup` + `check-bundle-size.mjs`), so the deletion left headroom rather than needing more — correctly not raised. No release-version bump, correct: `COST_REPORT_ENVELOPE_VERSION = 4` exists on no tag and not on `main`, and every one of the 8 consumers of the old `person_mapping_unusable` at `eb7a0a49` (contract doc, `01-cost/actions/03-report.md`, envelope, artefact, three tests, one fixture — exactly the set `plan.md` enumerates) is updated at `HEAD`. No secret, session id, prompt or absolute machine path enters the diff; the `"Baptiste"` display-name fixtures are pre-existing and their count did not rise (6→5 in `telemetry-identity.e2e.test.ts`, unchanged elsewhere) |

## Outcomes

| Sev | Location | Outcome |
| --- | -------- | ------- |
| 🔴 | `plan.md` Decisions row ("An identity file with no origin...") | Fixed. Row rewritten: neither file ever shipped (verified again: `git tag --contains 6c2b2a56` empty, `git merge-base --is-ancestor 6c2b2a56 main` → no, `git ls-tree -r main` has no identity file); the true asymmetry is that `identity.json` already exists on this branch's own working machines, written by this adapter's own shape, while `person-mapping.json` has no such file anywhere to migrate. "In the world" is now stated as this branch's working trees, never a released user. Resources row also corrected: `38de97d8` deleted two files (`skills/00-init/scripts/lib/identity.cjs`, `skills/00-init/scripts/telemetry-identity.cjs`); `40f13dcc` deleted the second copy at `skills/01-cost/scripts/lib/identity.cjs`. |
| 🟡 | `person-identity-use-case.ts` (`use`, `link`) | Fixed for the write path the diff itself could reach. Added `EmptyIdentifierError` (`cli/src/application/errors.ts`), reused across `use` and `link` the way `EmptyDisplayNameError` guards `name`. `use ""`, `use "   "`, `link ""`, `link "   "` all now refuse before touching the store, writing nothing - proven on the built CLI under a sandboxed `HOME`. `unlink` is deliberately left unguarded: once `link` refuses to write an empty identifier, `alsoMe` can never contain one, so `unlink("")` already returns `removed: false` through the existing `alsoMe.includes` check - adding a throw there would have contradicted `PersonIdentityUnlinkResult.removed`'s own doc comment ("never as a failure"). Tests added for `use`, `link` (empty refusal) and `unlink` (empty reported as nothing-to-remove, not a failure). **Not fixed, out of the scope handed to this pass:** a hand-edited `{"person_id":"","origin":"adopted"}` file (never written by any verb, only by hand) still reads back as `null` and is not removed by `off` - reproduced on the built CLI, file survives `off` with `already off - nothing to withdraw`. The review's fix text proposed closing this too ("make `off` remove a file that exists but reads back as `null`"); the task dispatched to this pass named only `use`/`link`/`unlink`, not `off`, so this route is left open and flagged here rather than silently claimed closed. |
| 🟡 | `cost-report-artefact.ts` `caveats()` + `cost-report-contract.md:323` | Fixed. `caveats()` now takes `includeAbsentIdentityCaveat`, defaulting to `false`; only `personArtefact` passes `true`. `absent` no longer prints on `total`/`day`/`step`/`model`/`tool`/`project`; `unreadable` still prints everywhere (real damage). Proven on the built CLI (`--axis total` and `--axis model` on an empty profile print no person caveat; `--axis person` still does; a corrupt identity file still prints `own identity could not be read` on `--axis total`). Contract doc's "partial" rule rescoped to `undated_records`/`unreadable_lines` only; `identity_unusable` reworded to "changes how records are labelled, never how many are counted", matching `by_person`'s own wording. Unit tests added pinning both the silence on `total` and the surviving `unreadable` caveat. |
| 🟡 | `person-identity-use-case.ts:61-66,130` / `person-resolution.ts:56-60` | Fixed via the review's proposed resolution. `link` now treats `identity === person.personId` as already listed (writes nothing, reports `alreadyListed: true`), matching `alreadyListed`'s doc comment. The now-unreachable `alsoMe.includes(personId)` branch in `resolvePerson`'s ternary is deleted, replaced with an unconditional `[identity.personId, ...identity.alsoMe]` and a comment explaining why the branch is gone. This also resolves the phase-1 finding below (self-link was the only way to reach that branch). Proven on the built CLI: `link <own personId>` reports "already listed", `also_me` stays absent from the file, and `off` afterward reports `0 added identifiers removed`. Test added: linking a person's own identifier reports already-listed and appends nothing. |
| 🟢 | `person-identity-adapter.ts:16-20`, `json-file.ts:1-8` | Fixed. Both comments rewritten to state their real, current reason (`resolveAiddConfigDir` isolates the deliberate `AIDD_USER_CONFIG_DIR` refusal beside `resolveHomeDir`; `json-file.ts` keeps raw-JSON/filesystem-error narrowing out of the adapter) and dropped the "a second reader/caller is not ruled out" speculative clause. `home-dir.ts:27-38`'s own doc comment on `resolveAiddConfigDir` was pulled into the same fix even though not separately listed as a finding: it still said "today, the identity file and the person mapping" and "so a mapping adapter reuses it", which the new comment in `person-identity-adapter.ts` now cites as the honest justification - left stale, that citation would point at rot. Reworded to name only today's caller. |
| 🟢 | `phase-1.md` / `phase-2.md` architecture projections | Fixed. `models/person-mapping.ts` and its test are no longer shown as deleted in phase-1's projection or task list (task 2.5 now says they are left standing for phase 2); phase-2's projection and task 1.3 now list both as deleted there, matching `66486466`, the commit that actually deletes them. |

## Follow-up

Findings not itemized above (functional/1 "every case the deleted test file carried still exists") are resolved as a side effect of the `link`-self-append fix: deleting the dead branch removes the untested path phase 1's criterion 6 was flagged against. All other 🟢 findings from the original table not listed here were left as originally reported — out of this pass's assigned scope.

### Validation after fixes (cli/)

- `pnpm typecheck` — clean
- `pnpm lint` (biome) — clean (2 pre-existing config-version infos only)
- `pnpm test` — 280 files, 3004 tests passed. Reconciled against the unpatched `HEAD` (`b7c28615`), measured in a disposable `git worktree` rather than by stashing: `pnpm test` there is already 280 files / 2998 tests, so this pass's net is +6 tests, exactly the six new `it` blocks added (four in `person-identity-use-case.unit.test.ts`, two in `cost-report-artefact.unit.test.ts`) - the review's earlier "251 files / 2778 tests" figure was a mutation-testing subset, not the full suite, and is not the right number to diff against
- `pnpm knip` — clean, no new findings (pre-existing unused-export list unchanged by this diff)
- `pnpm jscpd` — clean, no new clones introduced
- `pnpm build` — clean; bundle 565.6 KB / 590 KB budget
- repo-level `node --test "scripts/__tests__/**/*.test.js"` — 240 tests passed
- 7 pre-existing stash entries confirmed untouched throughout (`git stash list` — still 7)
