# Review: one route, and every sentence about it true

- **Verdict**: blocked
- **Diff**: `47f114d5...e449df9d`
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_28
- **Findings**: 3 critical, 6 warning, 4 minor

## Phases

### Phase 1 — A refusal, and a consent that is asked for

- [x] A person who refuses in their environment is not recorded, in a project whose tracked configuration allows it — `scripts/__tests__/aidd-telemetry-journal.test.js:1015` (end-to-end hook replay, `AIDD_TELEMETRY=0`, switch on, zero run files)
- [x] Refusing changes no file, and leaves the working tree clean — same test, `git status --porcelain` compared before/after
- [x] An unset variable turns nothing on by itself — `scripts/__tests__/aidd-telemetry-journal.test.js:970` (`undefined`, `""`, `"1"`, `"false"`, `"no"` all non-refusals)
- [x] The hooks and the CLI agree about whether measurement is on, for every combination of the two inputs — measured, 48/48 agree: `AIDD_TELEMETRY` ∈ {unset, "", "0", "1", "false", " 0", "00", "true"} × config ∈ {absent, unparseable, `{"telemetry":true}`, `{"telemetry":[{"enabled":true}]}`, `{"telemetry":{"enabled":true}}`, `{"telemetry":{"enabled":"true"}}`}, hook via `repo.cjs`'s `telemetryEnabled`, CLI via `cli/dist/cli.js telemetry report`. See finding 12 for the one CLI path that never asks.
- [x] Turning measurement on without confirmation is refused, and names the consequence — `cli/src/application/use-cases/telemetry/telemetry-on-use-case.ts:53`, reusing `cli/src/application/errors.ts:70`'s existing sentence
- [x] Turning it off needs no confirmation — `aidd telemetry off` run in a sandbox, no `--yes`, exit 0
- [x] Nothing in the output claims the journal names a person — `record.cjs` writes no identity field on any line (`buildSessionStartLine`, `buildTurnEndLine`, `buildFileWrittenLine`, `buildStepStartLine`, `buildTaskDeclaredLine`); grep for `user|author|email|person|identity` in `record.cjs` returns nothing

### Phase 2 — One route: the writing side goes

- [x] No command offered by the system starts a server or writes an export destination — `aidd telemetry --help` lists `on/read/identity/check/report/off`; `grep -rn "OTEL_\|CLAUDE_CODE_ENABLE_TELEMETRY" cli/src plugins` returns nothing
- [x] A removed command is refused as unknown, like any other unknown command — `receive` → `unknown command 'receive'`
- [x] Nothing in the source reads or writes a tool's export configuration — `export-config-reader.ts`, its adapter, and `claude-telemetry.ts` all deleted; no surviving reference
- [x] A stored record produced by the removed route is still read, counted and reported, with its figures — `cli/tests/e2e/telemetry-stored-export-record.e2e.test.ts`; reproduced independently, a `provenance:"export"` record seeded into a sandboxed sink reported `sessions 1 / requests 1 / 43,853 tokens / $0.01`
- [x] The three double-count rules still hold with both kinds of stored record present — `cli/tests/domain/models/cost-report.unit.test.ts` and the new e2e; `parseTelemetrySinkLine` and `telemetrySinkRecordDayKey` retained in `cli/tests/domain/models/telemetry-sink-record.unit.test.ts`
- [x] A check fails if a server is introduced anywhere in the CLI source — `scripts/__tests__/cli-opens-no-server.test.js`, four patterns over every tracked `cli/src/**/*.ts`, with a floor assertion so an empty walk cannot pass

### Phase 3 — The diagnostic grades only what exists

- [x] The diagnostic prints four claims, always in the same order — `cli/tests/domain/models/telemetry-claim.unit.test.ts:292`; observed live: `hook fired / session journalled / tool files readable / records join`
- [x] No claim mentions exporting, a destination, or an identity attribute — `telemetry-claim.unit.test.ts` "no claim mentions exporting…"
- [x] A working installation reports no failing claim — `cli/tests/e2e/telemetry-check.e2e.test.ts:204` asserts `not.toContain("FAIL")`
- [x] An installation with nothing measured yet reports nothing to evaluate, not a failure — `telemetry-check.e2e.test.ts:139-144`; observed live: three `--` lines under one genuine `FAIL`
- [x] No claim recommends a command that no longer exists — `telemetry-claim.unit.test.ts` "no claim recommends a command the system no longer offers"
- [x] The diagnostic skill states the claims the command prints, in the number it prints them — `plugins/aidd-telemetry/skills/02-check/SKILL.md:14`
- [x] A check fails if the two ever disagree again — proven by mutation: `four claims` → `six claims` in `SKILL.md:14` turned `cli/tests/domain/models/telemetry-claim.unit.test.ts:362` red (`- 4 / + 6`); reverted, tree clean. Coverage is partial — see finding 10.

### Phase 4 — The sentences become true

- [x] Two rows naming one step are distinguishable in the pasted table alone — `cli/src/application/display/cost-report-artefact.ts:194-213`, third `Attribution` column; observed in `--axis step` output
- [x] The rows for one step reconcile to what the terminal prints for that step — `cli/tests/application/display/cost-report-artefact.unit.test.ts` (+81); the sweep of the other axes is correct, only `by_step` and the attribution mix carry a second key (`cost-report-envelope.ts:50,95`)
- [ ] A report run with measurement off says so — only the terminal rendering does. `--json` and `--axis` say nothing: `toCostReportEnvelope` (`cost-report-envelope.ts:301`) never carries `measurementEnabled`. See finding 2.
- [ ] Nothing that was never measured is printed as a zero — `--json` with measurement off prints `"sessions": 0` and `"requests": 0`. See finding 2.
- [ ] Each of the five named sentences is true when checked against the code — four are. The plugin README's "Nothing leaves your machine, full stop — there is no command, no endpoint, and no route in this plugin that sends anything anywhere" is false for anyone who ran the now-deleted `aidd telemetry endpoint`. See finding 1.
- [ ] No shipped document names a removed command — `aidd telemetry off`'s own help text still reads "any endpoint configuration is left alone"; `metrics-contract.md` cites three deleted fixtures; `docs/FAQ.md` still carries the endpoint-era qualification. See findings 5, 6, 7.
- [x] The generated catalogue and prompt index match what their scripts produce — neither `plugins/aidd-telemetry/CATALOG.md` nor `docs/prompts-documentation.md` contains any of the changed strings (`six claims`, `four claims`, `what do we owe`, `telemetry endpoint`), so neither went stale

## Findings

| Sev | Kind | Phase | Location | Issue | Fix | Outcome |
| --- | ---- | ----- | -------- | ----- | --- | --- |
| 🔴 | fit | 2, 4 | `cli/src/application/commands/telemetry.ts:148`; deleted `telemetry-endpoint-clear-use-case.ts`, `enable-tool-telemetry-use-case.ts`; `plugins/aidd-telemetry/README.md:13,139` | A person who ran `aidd telemetry endpoint` before this change is **still exporting, and nothing can stop it or tells them**. Reproduced: with `.claude/settings.local.json` holding `CLAUDE_CODE_ENABLE_TELEMETRY=1` + `OTEL_EXPORTER_OTLP_ENDPOINT`, `aidd telemetry off` left both keys untouched and printed nothing about them; `aidd telemetry check` graded four local claims and never mentioned them; `.aidd/config.json` kept `telemetry.endpoint` verbatim. The writer and its targeted undo were deleted in the same commit. One remedy survives by accident, verified: `enable-tool-telemetry-use-case.ts:72,76` registered those keys as a `MergeFileEntry` on the tool's manifest, and `clean-use-case.ts:147-160` iterates exactly that shape and calls `removeEntriesFromJson(content, sectionKey, keys)`. Which means the only way left to stop exporting is to remove the entire AIDD installation, and nothing anywhere points at it. Meanwhile `README.md:13` now promises "Nothing leaves your machine, full stop" — false for exactly the population this change affects, and the reader most likely to trust it. `off`'s own help still says "any endpoint configuration is left alone", pointing at a command that no longer exists. | Either restore a targeted clearing path (`telemetry off` removing the keys it once wrote, or a one-shot migration), or — at minimum — have `off` and `check` detect a leftover export configuration, name the file and keys, and tell the person how to remove them by hand; and qualify `plugins/aidd-telemetry/README.md:13,139` for previously-configured installs until they can. | fixed — detection only, writer not resurrected. New `domain/models/telemetry-export-leftover.ts` (pure key detector) + `TelemetryEvidenceAdapter.findLeftoverExportConfig` (checks `.claude/settings.local.json`, `.claude/settings.json`, `~/.claude/settings.json`). `TelemetryOffUseCase` now warns on stderr naming the file and keys; `DiagnoseTelemetryUseCase`/`printTelemetryCheckReport` do the same, on both sides of the gate, never as a fifth claim. `telemetry.ts:148`'s description rewritten. Verified on the built CLI, sandboxed HOME, before/after: before, `off`/`check` said nothing about a `.claude/settings.local.json` holding `CLAUDE_CODE_ENABLE_TELEMETRY`/`OTEL_EXPORTER_OTLP_ENDPOINT`; after, both print `Warning: …/.claude/settings.local.json still sets CLAUDE_CODE_ENABLE_TELEMETRY, OTEL_EXPORTER_OTLP_ENDPOINT — delete these keys … by hand`, and the file itself is left byte-for-byte untouched. Unit tests: `telemetry-export-leftover.unit.test.ts`, `telemetry-off-use-case.unit.test.ts`, `diagnose-telemetry-use-case.unit.test.ts`. |
| 🔴 | functional | 4 | `cli/src/domain/models/cost-report-envelope.ts:301`; `cli/src/application/commands/telemetry.ts:133-135` | `--json` and `--axis` never say measurement is off, and print counts that were never measured as literal zeros. Measured with the switch on and `AIDD_TELEMETRY=0`: `--json` returned `"sessions": 0, "totals": {"requests": 0}` with no measurement field at all; `--axis step` printed a bare table header. Both violate the spec's hard constraint "A report says whether measurement is on. A count that was never measured is never printed as zero." The justification directly above the call is now false of its own file: *"One value, three renderings. None derives a figure the others cannot see"* — the terminal rendering reads `report.measurementEnabled`, which the envelope both other renderings consume does not carry. | Add `measurement_enabled` to `CostReportEnvelope` (a `cost_report_version` bump if the shape is pinned), render it in the artefact header, and delete or correct the "none derives a figure the others cannot see" comment. | fixed — `measurement_enabled` added to `CostReportEnvelope`/`toCostReportEnvelope`, no version bump (verified every consumer — `expected-envelope.json`, `03-report.md`, `cost-report-contract.md`, all in-repo tests — updated in this same commit, so "adding a field you may ignore" applies). Artefact header now appends `— this project's switch is off, figures are the whole sink, not scoped to it` on every axis when off. The "one value, three renderings" comment is true again — verified: `--json` before had no measurement field; after, `"measurement_enabled": false` present. `--axis step` before printed a bare header; after, the header carries the same clause. `cost-report-contract.md` and `01-cost/actions/03-report.md` updated to document the field. |
| 🔴 | fit | 4 | `cli/src/application/use-cases/telemetry/report-cost-use-case.ts:33-39,181-183`; `cli/src/application/display/cost-report-display.ts:373` | The report prints **"measurement is off for this project"** while reporting records it just measured. Reproduced: from a directory with no `.aidd/config.json`, against a sandboxed sink holding one record, the report printed the off line and then `sessions 1 / requests 1 / 43,853 tokens / $0.01`. The sink is person-scoped and spans every project; the switch now stapled to it is scoped to whatever directory the CLI happens to run from. This is the coupling introduced by giving a read-only report `projectRoot` and `env`. Phase 4 task 2 required the sentence and no phase specified its scope, so the implementer gave a person-scoped artefact a project-scoped claim: a report that used to depend only on the sink and the journal now makes a claim it has no standing to make, and gets it wrong in the ordinary case of reporting from anywhere but a measured project's root. | Either scope the sentence to what it can actually know ("this project is not being measured; the figures below are the whole sink"), or suppress it when the reported records name projects other than the one whose switch was read, or drop the project-scoped read from a person-scoped report and put it on `telemetry check`, which already owns it. | fixed — took the first option. `cost-report-display.ts`'s `printHeader` now prints "this project's own switch is off — the figures below are not scoped to it, they are the whole sink; turn this project's measurement on with `aidd telemetry on`", with the reasoning recorded in the comment above the call. Verified on the built CLI, before/after, same scenario as the finding's repro (no `.aidd/config.json`, sandboxed sink holding one record): before, `measurement is off for this project` sat directly above `sessions 1 / requests 1 / 1,000 tokens`; after, the reworded sentence names its own scope beside the same real figures. Regression test added: `cost-report-display.unit.test.ts` "names the sink's real scope, never denying the figure it sits beside". |
| 🟡 | rot | 2 | `cli/src/domain/models/telemetry-sink-retention.ts`; deleted `receive-telemetry-use-case.ts:100` | Sink retention was **silently lost** in the reviewed range: `decideTelemetrySinkRetention`'s only production caller was the deleted receive use case, so `aidd telemetry read` — the one remaining writer — pruned nothing and the sink grew without bound. Not dead code, a dropped feature: phase 2 authorised deleting the writing side, and retention was housekeeping that happened to live there. The implementer's report claims "knip — only pre-existing findings, none new"; that is false. Both this file and `telemetry-project-id.ts` became unused files in this range, and `knip --production` fails on them. Repaired after the reviewed range in `b3d777b1` (`read-local-cost-use-case.ts:275-305`); recorded here because the plan should have caught it. | None outstanding for retention. For the plan: an architecture projection that lists only the files a phase intends to delete cannot show what the deletion orphans; a `knip --production` gate inside the deletion phase would have. | not actioned — already repaired in `b3d777b1`, prior to this pass; explicitly named DO-NOT-TOUCH by the dispatching task. `knip --production` still clean (baseline captured before this pass matches after: 15 unused exports / 6 unused types, none touching retention or `telemetry-project-id.ts`). |
| 🟡 | rot | 4 | `aidd_docs/product/metrics-contract.md:85,86,191` | The contract still cites three fixture files phase 2 deleted (`otlp-logs-claude-code-subagent.json`, `otlp-metrics-claude-code.json`, `otlp-logs-claude-code.json`) as the live evidence for its own measured figures ($0.1605, $0.0151) and for its worked double-count example. `cli/tests/fixtures/telemetry-sink/` now holds only `expected.jsonl` and `opencode-export.json`. The contract's field-conformance test (`metrics-contract.unit.test.ts:68-85`) checks field names only, so nothing guards these citations. | Either restate those rows as "measured on a capture no longer in this repository, on <date>", or restore the fixtures read-only under a name that says they are historical evidence, not test input. | fixed — took the first option (restoring deleted fixtures would contradict phase 2's deliberate deletion). `metrics-contract.md:80-88` now states the fixtures were deleted with the export route and the figures are historical measurement "on a capture no longer in this repository"; the table's two fixture cells are marked `(deleted)`. `:193-194`'s worked example reworded the same way. `metrics-contract.unit.test.ts` unaffected (field-name check only) and still green. |
| 🟡 | conform | 4 | `docs/FAQ.md:45,49` | Missed by phase 4 task 3's sweep, and now the repository states two versions of one promise. FAQ still reads "nothing leaves your machine **on its own**" — a qualification that existed *only* because `aidd telemetry endpoint` could send — while `README.md:293` and `plugins/aidd-telemetry/README.md:13` now say it absolutely. FAQ:49 also still opens "Off unless you turn it on", the exact sentence `README.md:293` was corrected away from in this diff for hiding the git-tracked, everyone-who-clones consequence. | Carry the corrected `README.md:293` wording into `docs/FAQ.md:45` and `:49` verbatim. | fixed — `:45` dropped "on its own"; `:49` replaced verbatim with README.md:293's corrected sentence ("The switch is git-tracked, so it applies to everyone who clones; opt out per person with `AIDD_TELEMETRY=0`. Nothing leaves your machine."). `plugins/aidd-telemetry/README.md:13,139` also reworded — see the row above; neither claims an absolute "nothing leaves your machine" any more without naming the pre-existing-install exception. |
| 🟡 | rot | 1, 2 | `cli/src/application/commands/telemetry.ts:148`; `telemetry-off-use-case.ts:19-21`; `telemetry-on-use-case.ts:32-33,100`; `telemetry-switch.ts:12,66-67,79-81` | The `endpoint` concept survives the route in five places, three of them as doc comments asserting facts that are no longer true: `off`'s user-facing description ("any endpoint configuration is left alone"), "that is `aidd telemetry endpoint clear`'s job **now**", "that is `aidd telemetry endpoint`'s job". `TelemetrySwitch.endpoint` is still parsed and re-serialised by `parseTelemetrySwitchFile`/`buildTelemetrySwitchFile` and preserved by both `on` and `off`, for a key nothing in the system can set or clear. No phase authorised keeping it. | Preserving a stored `endpoint` key so an old config is not silently rewritten is defensible and matches the `provenance:"export"` reasoning — but say *that* at the declaration, and rewrite the three comments and the `off` description to stop naming commands that do not exist. | fixed — `telemetry-switch.ts`'s `endpoint` field now carries the "why preserved" doc comment at its declaration; `telemetry-off-use-case.ts` and `telemetry-on-use-case.ts`'s class comments rewritten to stop naming `aidd telemetry endpoint`/`endpoint clear`; `telemetry.ts:148`'s description rewritten (see row 1). `:66-67,79-81` are mechanical parse/serialise code, unchanged, now covered by the field's own doc. |
| 🟡 | functional | 4 | `aidd_docs/runs/README.md:19` vs `plugins/aidd-telemetry/hooks/lib/record.cjs:169` | **The sixth false sentence**, in the very table phase 4 task 3.3 set out to complete. The `file_written` row documents `path` alone; the hook writes `{ type, at, path, source }` on every such line. The change added the two missing line *types* and left a missing *field*, so "Where a document lists what the system writes, the list is complete" is still not satisfied. `source` is the privacy-relevant one: `record.cjs:162-166` states that `"observed"` "can, in principle, catch a file something else on the machine wrote in the same window" — a reader of the documented table has no way to know that field exists or that it can be filtered on. | Add `source` (`"tool-stated"` \| `"observed"`) to the `file_written` row with its meaning, and audit the other rows' fields the same way (`turn_id` on `step_start` is documented; confirm nothing else is omitted). | fixed — `file_written` row now lists `source` (`"tool-stated"` \| `"observed"`), with a new paragraph explaining what each value means and why `"observed"` is privacy-relevant. Audited every other row against `record.cjs`/`file-writes.cjs`: `session_start`, `turn_end`, `task_declared`, `step_start`, `scan_truncated` (`cap`, `scanned` — matches `file-writes.cjs:182`), and the `unrecognised_payload` marker all match their writer's actual fields; nothing else was missing. |
| 🟡 | code | 4 | `cli/src/domain/models/cost-report.ts:254-260` | The `?? true` default is safe but its justification is false. It cannot produce "measurement is on when it is off" in production — `ReportCostUseCase` is the only production caller of `buildCostReport` and always passes a concrete boolean, so `?? true` is reachable from tests alone. But the comment defends it with *"a report built from records already has to have come from somewhere that was measuring at least once"*, which is a claim about the past, while `CostReport.measurementEnabled`'s own doc at `:300` defines the field as "right now". Finding 3 is the counterexample the comment's reasoning cannot see. | Make the field required on `CostReportInput` and let the test helpers pass it, removing the default and the argument for it; or keep the default and rewrite the comment to say only what is true — no production caller omits this field. | fixed — took the first option. `measurementEnabled` is now required on `CostReportInput`, the `?? true` default and its comment deleted, `readFields` passes it straight through. All 8 test call sites across `cost-report.unit.test.ts`, `cost-report-envelope.unit.test.ts`, `cost-report-person.unit.test.ts`, `cost-report-artefact.unit.test.ts`, `cost-report-display.unit.test.ts` updated to supply it explicitly; typecheck enforces no site was missed. |
| 🟢 | rot | 3 | `cli/tests/domain/models/telemetry-claim.unit.test.ts:339-364` vs `plugins/aidd-telemetry/skills/02-check/actions/02-diagnose.md:25,37,43` | The drift guard reads `02-check/SKILL.md` only. `actions/02-diagnose.md` — the file phase 3 task 3.1 named as the one that hard-coded "all six claims" — states the count in prose three times ("Four claims", "all four claims", "a fifth failing claim") and is unguarded. The consumer that historically went stale is the one still able to. | Extend `statedClaimCount()` to scan both files, or move the count to one place both read. | fixed — `statedClaimCount()` replaced with `statedClaimCounts(path)`, using `matchAll` (not the first-match-only `match`) so all three cardinal mentions in `02-diagnose.md` are checked, plus `SKILL.md`. Mutation-verified: changing `02-diagnose.md`'s "Four claims" to "Six claims" turns the test red (`- 4 / + 6`); reverted, tree clean (`git status --porcelain` empty on that file). |
| 🟢 | rot | 2 | `cli/src/application/use-cases/telemetry/enable-tool-telemetry-use-case.ts` (+ its unit test), `cli/src/domain/ports/export-sink-reader.ts`, `cli/src/infrastructure/adapters/export-sink-reader-adapter.ts`, `cli/src/domain/tools/ai/claude-telemetry.ts` (+ its unit test), `cli/tests/e2e/telemetry-sink.e2e.test.ts`, `cli/tests/application/commands/telemetry-scope-parsing.unit.test.ts` | Eight of the 22 files deleted in this range were never named by phase 2's architecture projection, which listed the three use cases, two adapters, one port, their endpoint/receive tests and the OTLP fixtures. Every extra deletion is a correct consequence of removing the writer, so nothing is wrongly gone — but a projection that misses a third of what it authorises is how findings 1 and 4 escaped notice: the file that wrote (and whose sibling could clear) an export destination and the caller that pruned the sink both went out under a projection that mentioned neither. | Update phase-2.md's projection to what was actually deleted, so the record of what was authorised matches what happened. | fixed — `phase-2.md`'s architecture projection now lists all 22 deleted files (the 8 previously unnamed added to the tree), with a note explaining the correction and pointing at findings 1 and 4 as the consequence of the original gap. Projection block only; no task text or acceptance criterion touched. |
| 🟢 | fit | 1 | `cli/src/application/use-cases/telemetry/read-local-cost-use-case.ts:261` | `aidd telemetry read`, now the only writer of the sink, never consults the project switch or the refusal. Verified: with `telemetry.enabled: false` **and** `AIDD_TELEMETRY=0`, `read` ran to completion (exit 0) and stopped only because no journal existed. The refusal is enforced upstream, by the hook not writing a journal — real but single-layered, and `read --session-id <id>` bypasses the journal entirely. Pre-existing, not introduced here; it matters now because this is the last writer standing. | Have `read` resolve the same `isTelemetryEnabled(projectRoot, env)` the report and the diagnostic already do, and refuse with the reason, so the refusal holds on both writers rather than one. | fixed — `ReadLocalCostUseCase` now takes a required `projectRoot`/`env` and a `TelemetryEvidenceReader`, and refuses before a single reader runs (also closing the `--session <id>` bypass the finding named). Result carries `refusedReason`; the display layer relays it instead of the (false, in this case) "no session journalled" message. Verified on the built CLI, before/after, same scenario as the finding's repro (`telemetry.enabled: false` + `AIDD_TELEMETRY=0`): before, `read` proceeded to completion silently; after, it prints "measurement is refused — AIDD_TELEMETRY=0 or the project switch is off; nothing read, nothing stored" and exits 0, sink file mtime unchanged. Unit tests: "a refusal holds on the one writer left" (both a full sweep and a direct `--session` read write nothing). Two pre-existing e2e tests (`telemetry-identity.e2e.test.ts`) that called `read` without ever turning measurement on were exploiting exactly this gap; updated to turn the switch on first, matching real usage. |
| 🟢 | conform | 2 | `cli/tests/e2e/telemetry-stored-export-record.e2e.test.ts:—`, seeded from `cli/tests/fixtures/telemetry-sink/expected.jsonl` | Public-repository check is otherwise clean: no absolute path from this machine, no personal name, no email, no prompt or code content anywhere in the diff; the run ids in `2026_08_28_telemetry-challenge.md` are paired with obviously synthetic vendor ids (`aaaaaaaa-…`, `11111111-…`). Advisory only: the vendor/turn UUIDs the new e2e reuses (`7c53f826-…`, `a4b7b0b6-…`) are unpatterned and read as real Claude Code identifiers captured from a session. They pre-exist at `47f114d5` in `expected.jsonl`, so this diff copies rather than introduces them. | If they are genuine capture ids, replace them repo-wide with patterned placeholders in a separate change; nothing to do in this one. | not actioned — advisory only, marked "nothing to do in this one" by the finding itself; not among the 9 fixes this pass was dispatched to make. |

## Verification

| Metric        | Value                                             |
| ------------- | ------------------------------------------------- |
| Verified      | 85% (23/27)                                       |
| Files checked | `cli/src/domain/models/{telemetry-switch,cost-report,cost-report-envelope,telemetry-claim,telemetry-sink-retention,telemetry-project-id}.ts`, `cli/src/application/use-cases/telemetry/{report-cost,read-local-cost,telemetry-on,telemetry-off,diagnose-telemetry}-use-case.ts`, `cli/src/application/display/{cost-report-display,cost-report-artefact}.ts`, `cli/src/application/commands/telemetry.ts`, `cli/src/infrastructure/{deps.ts,adapters/telemetry-evidence-adapter.ts,home-dir.ts}`, `cli/src/domain/ports/telemetry-evidence-reader.ts`, `plugins/aidd-telemetry/hooks/lib/{repo.cjs,record.cjs}`, `plugins/aidd-telemetry/README.md`, `plugins/aidd-telemetry/skills/{00-init,01-cost,02-check}/**`, `README.md`, `docs/FAQ.md`, `aidd_docs/runs/README.md`, `aidd_docs/product/{metrics-contract,cost-report-contract}.md`, `cli/tests/e2e/{telemetry-refusal,telemetry-stored-export-record,telemetry-check}.e2e.test.ts`, `cli/tests/domain/models/telemetry-claim.unit.test.ts`, `scripts/__tests__/{cli-opens-no-server,aidd-telemetry-journal}.test.js` |
| Unchecked     | Phase 4 "a report run with measurement off says so" — fix, finding 2; Phase 4 "nothing never measured is printed as a zero" — fix, finding 2; Phase 4 "each of the five named sentences is true" — fix, finding 1; Phase 4 "no shipped document names a removed command" — fix, findings 5, 6 and 7 |
| Unplanned     | Eight files deleted beyond phase 2's projection (finding 11); sink retention orphaned and `telemetry-project-id.ts` left unused, neither traced to a criterion (finding 4); `ReportCostUseCase` gaining `projectRoot` and `env` — the mechanism phase 4 task 2 was implemented through, chosen without any phase stating the sentence's scope (finding 3) |
