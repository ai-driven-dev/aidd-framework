# Review: the deletion path

- **Verdict**: blocked
- **Diff**: `92e9c889...983edca1`
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_31
- **Findings**: 4 critical, 6 warning, 2 minor

## Phases

### Phase 1 — What would go, and what cannot

- [x] Every location carries what it is, where it is, and how much is in it — `cli/src/domain/models/telemetry-removal.ts:22-52`
- [x] A project's journal and a machine's records cannot be rendered as the same kind of thing — `scope` discriminant at `telemetry-removal.ts:23,34,45`; renderer splits on it at `telemetry-forget-display.ts:57-63`
- [ ] A tracked journal reads as history certainly holding it — over-asserted. `git ls-files` reads the **index**, not history. Measured: a repo with zero commits, `git add aidd_docs/runs/A.jsonl` → `git ls-files -- aidd_docs/runs/` lists it → preview would say "history certainly holds it" about a file no history contains
- [x] An untracked journal reads as history possibly holding it, never as clear — sandbox run: "not tracked by git right now, but history may still hold it if it was ever committed before — that cannot be told apart from never having been committed"
- [x] A location that cannot be read is reported and still listed for removal — sandbox: `2026-08-29.jsonl` containing `DAMAGED{{{` listed and removed; `listRunFiles`/`listDayFiles` are `readdir`+extension filter only, never `readFile` (`run-journal-reader-adapter.ts:214-221`, `telemetry-sink-adapter.ts:169-176`)
- [x] A preview removes nothing, verified against the filesystem — sandbox inventory hash `6f6eef62…` identical before and after `telemetry forget` with no `--yes`
- [x] A machine where nothing was measured says so and offers nothing — sandbox non-git empty dir: "nothing was ever measured here … Nothing to remove", exit 0

### Phase 2 — Removing it, and saying what happened

- [x] The journal's files are removed and nothing else in its directory is — sandbox: 8 `.jsonl` entries taken, `keep-me.txt` survived; `aidd_docs/other/notes.md` and `~/.config/aidd/other/keep.json` survived
- [ ] The removal acts on the value the preview produced, and cannot resolve its own locations — **false for two of three locations**. `deleteRunFile` calls `resolveRunsDir()` again (`run-journal-reader-adapter.ts:224-226`) and `PersonIdentityAdapter.forget()` calls `identityFilePath()` again (`person-identity-adapter.ts:141-147`). Only `TelemetrySinkAdapter` freezes `rootDir` in its constructor. Proven by mutation, below
- [x] A location that refuses removal is reported, and every other location is still emptied — real CLI, no doubles: a directory named `adir.jsonl` → `Could not remove journal run file adir.jsonl — … EISDIR`, and the other 7 run files, both day files and the identity were still removed
- [x] The switch is untouched, and measurement can be turned on again afterwards — `.aidd/config.json` md5 `245ae127ed556c3b40c5299c3fcf25cf` identical before and after; `aidd telemetry on --yes` afterwards exits 0
- [x] Without confirmation nothing is removed, and the command exits successfully — "Nothing removed. Pass --yes…", exit 0, inventory unchanged
- [ ] The counts reported match what the preview showed — journal and sink match (preview 8 → 7 removed + 1 failed; 2 → 2). Identity does **not**: `removeIdentity` never reads `preview.identity.present` (`forget-telemetry-use-case.ts:148-156`) and reports a fresh filesystem answer. Measured: preview `present=false`, remove reported `identity.removed = 1`
- [x] What cannot be reached is repeated after removing — `printTelemetryForgetResult` calls `printHistory` (`telemetry-forget-display.ts:104`), with `result.history` carried through unchanged from the preview

### Phase 3 — A person who asks is answered

- [x] A person asking to have their measurement removed reaches this command through the skill — `SKILL.md` description gains "wants their measured data deleted"; third flow `ask3([asks to be forgotten])`; `actions/05-forget.md:34-48`
- [x] The skill's instructions relay what cannot be reached — `05-forget.md` step 3.1, both strengths, "never as an all-clear", "no command here rewrites git history"
- [ ] No shipped document still says what was recorded cannot be removed — `README.md` fixed; the CLI's own `aidd telemetry off` output still ends "sessions already journalled stay in `aidd_docs/runs/` and whatever `aidd telemetry read` already stored" and names no way to remove them
- [x] A guard fails when the skill's account and the command disagree — verified by mutation in both directions: dropping `--yes` from `05-forget.md` fails "names both the preview and the confirmed removal"; renaming `command("forget")` in `dist/cli.js` fails the sweep with `unknown command 'forget'`. Both restored
- [x] The generated catalogue and prompt index match what their scripts produce — `CATALOG.md` and `docs/prompts-documentation.md` carry exactly the new `SKILL.md` description string

## Findings

| Sev | Kind | Phase | Location | Issue | Fix | Outcome |
| --- | ---- | ----- | -------- | ----- | --- | ------- |
| 🔴 | functional | 2 | `cli/src/infrastructure/adapters/run-journal-reader-adapter.ts:224`, `cli/src/infrastructure/adapters/person-identity-adapter.ts:141` | The load-bearing guarantee is false for the journal and the identity. `remove()` consumes only file *names* from the preview; the *directory* each name is joined to is re-resolved inside the adapter. Proven: with `AIDD_RUNS_DIR` relocated after `runsDir` was read, the file the preview showed **survived** and a file in a different directory was **deleted**. Same for the identity with `HOME` relocated after the preview. Spec done-when demands "the same value, not two agreeing computations — so no input can make one differ from the other, including a path relocated by configuration" | Resolve each location once into the preview and have `remove()` act on `preview.journal.path` / `preview.identity.path`, e.g. `deleteRunFile(dir, fileName)` and `forget(path)`. Freeze the value the way `TelemetrySinkAdapter` already freezes `rootDir` in its constructor | **Fixed.** `deleteRunFile(dir, fileName)` and `TelemetrySink.deleteDayFile(dir, fileName)` and `PersonIdentityStore.forget(path)` all now take the directory/path explicitly, threaded from `preview.journal.path` / `preview.sink.path` / `preview.identity.path` in `ForgetTelemetryUseCase.removeJournal/removeSink/removeIdentity` — never re-resolved inside the adapter. `runsDir`/`filePath` are also frozen at construction (belt-and-suspenders, matching the sink). Proven by mutation: reproduced the exact bug pre-fix (file shown survived, a different-directory file was deleted, for both the journal and the identity), then proved it gone post-fix, at both the raw-adapter level and via `ForgetTelemetryUseCase` handed a preview naming a relocated path. New tests: `run-journal-reader-adapter.integration.test.ts` ("acts on the dir it is handed, immune to AIDD_RUNS_DIR being relocated afterwards"), `person-identity-adapter.integration.test.ts` (new file, "acts on the path it is handed, immune to HOME being relocated afterwards"), `forget-telemetry-use-case.unit.test.ts` (two new "proves the guarantee by mutation" tests, one per location), and a new e2e case for a relocated `AIDD_RUNS_DIR`. |
| 🔴 | code | 2 | `cli/src/application/use-cases/telemetry/forget-telemetry-use-case.ts:148-156` | `removeIdentity` ignores `preview.identity.present` and calls `identity.forget()` unconditionally, deleting whatever is on disk at removal time and reporting a count derived from the filesystem, not from what the person saw. Proven: preview reported `present=false` ("nothing to remove"), a file appeared, `remove()` deleted it and reported `1 removed`. This is the one place a removal is documented as impossible to reach past the preview, and it is the one place that does not consult it | Gate on the preview: `if (!identity.present) return { removed: 0, failed: [] }`, so an identity never shown is never removed and the count always traces to the preview | **Fixed**, exactly as suggested — `removeIdentity` now returns `{ removed: 0, failed: [] }` immediately when `!identity.present`, never calling `this.identity.forget()` at all. Proven by mutation: reproduced the exact bug (preview `present=false`, a file appears, old code reported `1 removed`), then proved the fixed code reports `{removed:0,failed:[]}` and `forgetCount` stays `0`. New test: `forget-telemetry-use-case.unit.test.ts` — "never touches an identity that was not shown in the preview — the gate on preview.identity.present". |
| 🔴 | code | 1 | `cli/src/domain/models/telemetry-removal.ts:55-62`, `cli/src/application/display/telemetry-forget-display.ts:24-31`, `plan.md` "History is reported at two strengths" | False user-facing certainty. `git ls-files` answers what is in the **index**, not what is in history; a journal `git add`ed and never committed is listed. The shipped string asserts "history certainly holds it" and the skill relays it as certain. Measured in a repository with zero commits. This is the false-certainty fault the plan names as the one it exists to prevent, inverted: the *strong* reading is the one that over-asserts | Either check history rather than the index (`git log --oneline -1 -- <path>` / `git rev-list --count HEAD -- <path>`), or weaken the wording to what `ls-files` can support: tracked now, so it will be in history from the next commit and is in history if it has ever been committed | **Fixed** by adding real history detection, not just wording. New `VersionControl.hasHistoryFor(repoRoot, pathspec)` (`git log --oneline -1 -- <pathspec>`) plus the existing `isRepository`. `TelemetryHistoryReading` now has four honest states — `"committed"` (tracked AND history actually holds a commit for it), `"staged"` (tracked but never committed — the index still holds the blob after this removal, so a bare `git commit` afterward would restore it, which the message now says), `"possible"` (untracked, unchanged), `"none"` (not a repository at all, finding 8's fix folded in here). Reproduced the false claim in a zero-commit repo via the built CLI (`this project's run journal is tracked by git right now, so history certainly holds it`), then proved the fixed CLI reports `"Cannot be reached, not yet: … has never been committed"` for the same repro. Corrected the same false claim in `plan.md`'s Decisions row, `SKILL.md`, and `05-forget.md` (both the prose and the Test table). New tests: 2 new unit cases (`"staged"`, `"none"`) plus 1 new e2e case (zero-commit staged journal) in addition to the existing tracked/possible cases, which still pass unchanged. |
| 🔴 | code | 2 | `cli/src/infrastructure/adapters/run-journal-reader-adapter.ts:218-226`, `cli/src/infrastructure/adapters/telemetry-sink-adapter.ts:178-180` | Escape outside the directory is **expressible**, and the comment that says it is not states a false mechanism: "`join` with `resolveRunsDir()` alone … so a name this method deletes always sits inside the one directory". `join` normalises, it does not confine. Proven: `deleteRunFile("../../VICTIM.txt")` deleted a file outside the runs directory. Spec: "must be impossible to express, not merely absent". Not reachable from disk today — 8 adversarial on-disk names (`..jsonl`, `.jsonl`, embedded newline, backslash, `; rm -rf ~ .jsonl`, a directory, a symlink) all stayed inside, and the symlink's target outside survived — but the confinement comes from `readdir` yielding bare components, not from the stated mechanism | Confine explicitly before deleting: reject any name where `basename(name) !== name`, or verify `resolve(dir, name)` still has `dir` as its parent. Correct the comment to name `readdir` as the actual guarantee | **Fixed**, both locations. New `isBareFileName` in `cli/src/infrastructure/confined-file-name.ts` (`basename(name) === name`, excluding `""`, `"."`, `".."`), used by both `deleteRunFile` and `deleteDayFile` (cheap, done here too as the finding allowed) — a name failing it throws before `rm` is ever called. Both doc comments corrected to name this check as the real guarantee, not `readdir`. Proven by mutation: reproduced `deleteRunFile("../../VICTIM.txt")` deleting the victim pre-fix, then proved it throws and the victim survives post-fix. New tests: `run-journal-reader-adapter.integration.test.ts` (relative-walk refusal, bare `.`/`..` refusal) and `telemetry-sink-adapter.integration.test.ts` (relative-walk refusal, mirroring the same check). |
| 🟡 | code | 2 | `cli/tests/application/use-cases/telemetry/forget-telemetry-use-case.unit.test.ts:229`, `cli/tests/e2e/telemetry-forget.e2e.test.ts:268` | The two tests that exist to prove the structural guarantee both exercise the **sink** — the only one of the three locations that actually holds it — and the e2e relocation test uses `AIDD_USER_CONFIG_DIR`, the only variable that is frozen at construction. Nothing covers `AIDD_RUNS_DIR` relocation or the identity's independence from the preview, which is why the two broken paths shipped green | Add the same mutation for the journal and the identity: relocate `AIDD_RUNS_DIR` / `HOME` between `preview()` and `remove()` and assert the shown location is the one emptied | **Fixed.** Added the sink test's mutation shape for the journal and the identity in `forget-telemetry-use-case.unit.test.ts` (hand `remove()` a preview naming a relocated path; assert the in-memory double received exactly that path/dir, never its own). Added a real e2e case for a relocated `AIDD_RUNS_DIR` mirroring the existing `AIDD_USER_CONFIG_DIR` one. Added two new real-adapter integration test files/cases proving the same guarantee against actual `AIDD_RUNS_DIR`/`HOME` relocation on real disk (`run-journal-reader-adapter.integration.test.ts`, new `person-identity-adapter.integration.test.ts`). |
| 🟡 | rot | 2 | `cli/tests/e2e/telemetry-forget.e2e.test.ts:11-18` | The file's own header claims it covers "a refused deletion leaving the rest untouched". It does not — the only match is "without --yes, refuses", a person declining, an unrelated case. The refused-deletion path is covered by in-memory doubles only. The stated reason for that limitation (no chmod-based e2e precedent, Windows fragility) is refuted: a **directory** named `*.jsonl` yields `EISDIR` on POSIX and refuses removal on Windows too, needs no `chmod`, and I ran it through the real CLI end to end with the correct per-file failure report | Correct the header to what the file covers, and add the directory-named-`*.jsonl` e2e — the real failure path is portable and one line to seed | **Fixed.** Header corrected to name the real cases the file covers (including the two false claims — the missing refused-deletion case and the sink-only mutation claim, both named explicitly). Added "a run file that refuses removal (a directory named \*.jsonl) is reported, and the rest is still removed" through the real built CLI — seeds a directory named `adir.jsonl`, runs `forget --yes`, asserts the per-file failure (`EISDIR`-shaped), the other two locations still empty, and the offending directory itself survives untouched. |
| 🟡 | fit | 3 | `cli/src/application/display/telemetry-display.ts` (`telemetry off` output) | The person at the exact moment the spec's Context describes — told where their records are, wanting them gone — is still given no next step. `aidd telemetry off` names both locations and never mentions `aidd telemetry forget`. `README.md` was updated; the CLI message a person actually reads was not. Phase 3's sweep was run over documents but not over shipped output | Append one sentence to the `off` message naming `aidd telemetry forget` as what removes them | **Fixed**, exactly as suggested — `printTelemetryOffReport` now appends "Run \`aidd telemetry forget\` to remove what was already measured." No test pinned the old exact string; `telemetry.e2e.test.ts`'s `already off` assertion still passes unchanged. |
| 🟡 | rot | 2 | `cli/src/domain/ports/run-journal-reader.ts:100-135` | A port named `RunJournalReader`, whose class doc says "Reads a session's run journal", now deletes. The repo already solved this exact problem its own way: `PersonIdentityReader` for reading, `PersonIdentityStore extends PersonIdentityReader` for the verbs that mutate. That precedent was not followed, and `diagnose-telemetry-use-case.ts:99`, `report-cost-use-case.ts:174` and `read-local-cost-use-case.ts:270` — three read-only use-cases — are now each handed a port that can delete a person's journal | Split it the way identity is split: keep `RunJournalReader` read-only, add `RunJournalStore extends RunJournalReader` carrying `runsDir` and `deleteRunFile`, and inject the store only into `ForgetTelemetryUseCase` | **Fixed**, exactly as suggested. `RunJournalReader` is now `read`/`list`/`listRunFiles` only (matching `PersonIdentityReader`'s shape); `RunJournalStore extends RunJournalReader` adds `runsDir` and `deleteRunFile(dir, fileName)`. `RunJournalReaderAdapter implements RunJournalStore`; `ForgetTelemetryUseCase`'s constructor is now typed `RunJournalStore`, while `diagnose-telemetry-use-case.ts`, `report-cost-use-case.ts` and `read-local-cost-use-case.ts` keep the narrower `RunJournalReader` type unchanged (they only ever called `.list()`/`.read()`, confirmed by grep) — none of the three can now be handed something that deletes, at the type level. |
| 🟡 | conform | 1 | `phase-1.md` architecture projection | The projection marks `cli/src/domain/ports/telemetry-sink.ts` and `cli/src/infrastructure/adapters/telemetry-sink-adapter.ts` as modified (✏️). Neither appears in `git diff --stat 92e9c889..HEAD` — the sink primitives already existed, as `plan.md`'s own Resources table says. The plan and the delivery disagree about what was touched | Correct the projection to match what shipped, or state that the sink needed no change | **Fixed.** Removed both `telemetry-sink.ts` and `telemetry-sink-adapter.ts` from the architecture projection and left a comment naming why (they were never touched by the original phase-1..3 diff being reviewed; the sink work in *this* remediation is a separate, later change, tracked here instead). |
| 🟡 | code | 3 | `cli/tests/e2e/helpers.ts:135-137` | Latent, not live: `sandboxedEnv`'s `realHome: true` branch sandboxes only `AIDD_USER_CONFIG_DIR` and leaves `HOME` real, so a CLI run through it resolves the identity file to the developer's **real** `~/.config/aidd/identity.json` (`resolveAiddConfigDir` reads `HOME`, never `AIDD_USER_CONFIG_DIR` — by design, `home-dir.ts:29-40`). This diff put `aidd telemetry forget --yes` into a sweep whose command list is scraped from markdown (`telemetry-init-skill-commands.e2e.test.ts:35-50`). Verified safe today: no test anywhere passes `realHome: true`, the sweep uses the fully sandboxed default, and the real profile's telemetry files are byte- and mtime-identical after this review. But nothing stops the next `realHome` test from also naming `forget` | Make the destructive command refuse to run against a non-sandboxed home in tests, or have `realHome: true` also redirect `HOME`; at minimum assert in the sweep that the resolved identity path is inside `fakeHome` before running `forget --yes` | **Judged and fixed, partially.** Verdict: yes, the combination should be impossible, not merely unused — `realHome` exists for network-tool tests (gh/git/npm), never for telemetry, and nothing tied it to `forget` before this. Added a guard in `runCli` (`cli/tests/e2e/helpers.ts`) that throws when `args` includes `"forget"` and `options.realHome` is true. This is a denylist keyed on one command name, not a structural fix for "any destructive command under `realHome`" — flagging that limitation rather than presenting it as fully closed, per the task's own framing. Full e2e/unit/integration suite re-run green after the change; no existing test passes `realHome: true`, so nothing was affected. |
| 🟢 | code | 1 | `cli/src/application/display/telemetry-forget-display.ts:33-38` | In a directory that is not a git repository at all, the preview still warns "history may still hold it if it was ever committed before". There is no history. Measured in a non-git sandbox directory. Over-warning rather than under-warning, so harmless, but it states a possibility that cannot exist | Use `VersionControl.isRepository` — already a port method — to say "not a git repository, so no history holds it" | **Fixed** as part of finding 3's `"none"` state — `printHistory` now says "This project is not a git repository, so no history holds this project's run journal." when `VersionControl.isRepository` answers false. |
| 🟢 | code | 1 | `cli/src/application/display/telemetry-forget-display.ts:53-62` | The preview reports counts only, never names. A person cannot check *which* files are about to go, only how many, and the post-removal report names a file only when it fails. For an irreversible act the spec calls "the worst failure this can have", the names are cheap and already in hand on `preview.journal.runFileNames` | Optionally list the names, or add `--verbose` handling that does | **Not done.** Out of the scope handed to this remediation (the 4 critical / 6 warning findings plus the 2 minors named explicitly as "do these too" in the dispatch — this row was not among them). Left for a follow-up; noted here rather than silently skipped. |

## Verification

| Metric        | Value                                             |
| ------------- | ------------------------------------------------- |
| Verified      | 79% (15/19)                                       |
| Files checked | `cli/src/domain/models/telemetry-removal.ts`, `cli/src/domain/models/paths.ts`, `cli/src/domain/ports/run-journal-reader.ts`, `cli/src/domain/ports/telemetry-sink.ts`, `cli/src/domain/ports/person-identity-store.ts`, `cli/src/application/use-cases/telemetry/forget-telemetry-use-case.ts`, `cli/src/application/use-cases/telemetry/telemetry-on-use-case.ts`, `cli/src/application/display/telemetry-forget-display.ts`, `cli/src/application/commands/telemetry.ts`, `cli/src/infrastructure/adapters/run-journal-reader-adapter.ts`, `cli/src/infrastructure/adapters/telemetry-sink-adapter.ts`, `cli/src/infrastructure/adapters/person-identity-adapter.ts`, `cli/src/infrastructure/adapters/git-adapter.ts`, `cli/src/infrastructure/home-dir.ts`, `cli/src/infrastructure/deps.ts`, `cli/tests/e2e/telemetry-forget.e2e.test.ts`, `cli/tests/e2e/telemetry-init-skill-commands.e2e.test.ts`, `cli/tests/application/use-cases/telemetry/forget-telemetry-use-case.unit.test.ts`, `cli/tests/domain/models/telemetry-removal.unit.test.ts`, `cli/tests/helpers/ports/in-memory-run-journal-reader.ts`, `cli/tests/helpers/ports/in-memory-person-identity-store.ts`, `plugins/aidd-telemetry/skills/00-init/SKILL.md`, `plugins/aidd-telemetry/skills/00-init/actions/05-forget.md`, `plugins/aidd-telemetry/README.md`, `plugins/aidd-telemetry/CATALOG.md`, `docs/prompts-documentation.md` |
| Unchecked     | P1 "a tracked journal reads as history certainly holding it" — fix; P2 "the removal acts on the value the preview produced, and cannot resolve its own locations" — fix; P2 "the counts reported match what the preview showed" — fix; P3 "no shipped document still says what was recorded cannot be removed" — fix |
| Unplanned     | `RunJournalReader.runsDir` getter (`run-journal-reader-adapter.ts:196-198`) traces to no phase-2 task — phase 2 asked only for a removal on the port; the extra read member is what phase 1's `journal.path` needed and is unstated in either phase. `RUNS_ENTRY` extraction into `paths.ts` and the `telemetry-on-use-case.ts` call-site change trace to phase 1 task 2.3 only implicitly |
