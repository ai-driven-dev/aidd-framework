# Review: resolve one person across tools and machines

- **Verdict**: changes-requested
- **Diff**: `a9b0d99d...f5f635ef`
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_28
- **Findings**: 1 critical, 6 warning, 5 minor

## Phases

### Phase 1 — The mapping and what resolving means

- [x] A mapping entry can be built with no display name, and reading it back shows no display name rather than an empty one — `cli/src/domain/models/person-mapping.ts:20` (`displayName?`), `cli/src/domain/models/person-mapping.ts:73` (spread-omitted, never `undefined`), test `cli/tests/domain/models/person-mapping.unit.test.ts:84`
- [x] An identity listed under a person, and that person's own identifier, resolve to the same canonical person — `cli/src/domain/models/person-mapping.ts:50-53`, tests `cli/tests/domain/models/person-mapping.unit.test.ts:27` and `:35`
- [x] A record with no identifier resolves to `none`, and one with an unlisted identifier resolves to `unresolved`, and the two are distinguishable — `cli/src/domain/models/person-mapping.ts:69-71`, tests `:50`, `:57`, `:71`; mutation-verified (returning `unresolved` for the empty branch fails 6 tests)
- [x] A resolved person carries back every identity behind it, including its canonical one — `cli/src/domain/models/person-mapping.ts:76-78`, test `cli/tests/domain/models/person-mapping.unit.test.ts:75`
- [x] A mapping where two people claim one identity is refused by name, and neither claimant is ever returned — `cli/src/domain/models/person-mapping.ts:102-112`, `cli/src/domain/errors.ts:532-542`, test `:99`
- [x] The same identity written twice inside one person resolves to that person without a refusal — `cli/src/domain/models/person-mapping.ts:104` (`new Set([personId, ...identities])`), test `:121`
- [x] A reader that has no mapping answers `null`, and no caller treats that as a failure — `cli/src/domain/ports/person-mapping-reader.ts:16-17`, `cli/tests/helpers/ports/in-memory-person-mapping-reader.ts`, `cli/src/application/use-cases/telemetry/report-cost-use-case.ts:86-91`

### Phase 2 — The person breakdown in the report

- [x] Two identifiers one person declared produce one row, not two — `cli/src/domain/models/cost-report.ts:489-491`, test `cli/tests/domain/models/cost-report-person.unit.test.ts:62`
- [x] Two identifiers nobody declared produce two rows, never one merged bucket — `cli/src/domain/models/cost-report.ts:492-496`, test `:102`; mutation-verified (collapsing the `unresolved` branch to `NO_KNOWN_PERSON` fails 5 tests)
- [x] Records carrying no identifier land in a row distinct from every unresolved one — `cli/src/domain/models/cost-report.ts:468` (`Symbol`, never a string key), test `:119`
- [x] Every person row lists the identifiers behind it, and a mapped row lists its canonical one among them — `cli/src/domain/models/cost-report.ts:927-935`, `person-mapping.ts:76-78`, test `:76`
- [x] Summing every person row's totals equals the report's own total — tests `:132` (requests) and `:147` (cost, input tokens); mutation-verified (dropping records inside `addToPersonGroup` fails both). Output and cache token totals are never summed — see finding 12
- [x] An envelope built with no mapping carries every identifier as unresolved, with the figures unchanged — `cli/src/domain/models/person-mapping.ts:70`, test `:184`
- [x] The envelope's report version is higher than before, and the reason is written beside it — `cli/src/domain/models/cost-report-envelope.ts:20-24` and `:31`, mirrored in `aidd_docs/product/cost-report-contract.md:112-118`

### Phase 3 — Reading, declaring and rendering it

- [x] The mapping is read from the OS profile even when `AIDD_USER_CONFIG_DIR` points elsewhere — `cli/src/infrastructure/home-dir.ts:41-46`, `cli/src/infrastructure/adapters/person-mapping-adapter.ts:29`; unit test `cli/tests/infrastructure/adapters/person-mapping-location.unit.test.ts:78` kills a mutation that honours the variable; independently confirmed by running the built CLI with `HOME` sandboxed and `AIDD_USER_CONFIG_DIR` pointed at a decoy holding a valid mapping (rows stayed `unresolved`; the same mapping under the profile resolved to `mapped`). The e2e that claims the same thing asserts nothing — finding 3
- [x] An absent mapping reads as none declared, and an unreadable one is surfaced rather than silently treated as none — `cli/src/infrastructure/adapters/person-mapping-adapter.ts:93-113` (`read` folds, `readStrict` throws `UnreadablePersonMappingFileError`), e2e `cli/tests/e2e/telemetry-person-mapping.e2e.test.ts:201`. No test drives the adapter's own `read`/`readStrict` directly; only its `filePath` is unit-tested
- [x] Linking before opting in is refused, naming the step that is missing — `cli/src/application/use-cases/telemetry/person-mapping-use-case.ts:48-49`, `cli/src/application/errors.ts:107-112`, test `cli/tests/application/use-cases/telemetry/person-mapping-use-case.unit.test.ts:13`
- [x] Linking an identity another person claims is refused, and the mapping is left as it was — `cli/src/infrastructure/adapters/person-mapping-adapter.ts:127-128` (validate before write), test `:45`
- [x] Unlinking an identity nobody listed reports nothing to remove and exits successfully — `cli/src/application/use-cases/telemetry/person-mapping-use-case.ts:57-62`, `cli/src/application/display/telemetry-display.ts:180-183`, test `:59`
- [x] Identity status lists every mapped identity, and says where the mapping was read from — `cli/src/application/use-cases/telemetry/person-identity-use-case.ts:100-112`, `cli/src/application/display/telemetry-display.ts:123-128`, tests `cli/tests/application/use-cases/telemetry/person-identity-use-case.unit.test.ts:54` and `:84`, e2e `:189`
- [x] `--axis person` prints one row per person with the identities behind it — `cli/src/application/display/cost-report-artefact.ts:216-230` and `:269`, test `cli/tests/application/display/cost-report-artefact.unit.test.ts:64`
- [x] An unreadable mapping still prints every figure, with a caveat stating the resolution was lost — `cli/src/application/display/cost-report-artefact.ts:136-138`, test `:113`, e2e `:201`
- [x] An unknown axis is refused by name, and `person` appears among the ones offered — `cli/src/application/display/cost-report-artefact.ts:26-34` and `:279-281`, test `:58`; the CLI help string is derived from `ARTEFACT_AXES` at `cli/src/application/commands/telemetry.ts:133`, so it cannot drift
- [x] Two unplaced identifiers print two labelled rows, never one bucket — `cli/src/application/display/cost-report-artefact.ts:48-51` and `:208-212`, test `:76`; mutation-verified
- [x] Turning the identity off prints that the mapping still lists it, and names the command that removes it — `cli/src/application/use-cases/telemetry/person-identity-use-case.ts:127-133`, `cli/src/application/display/telemetry-display.ts:158-161`, test `:197`, e2e `cli/tests/e2e/telemetry-identity.e2e.test.ts:164`

### Phase 4 — The journeys that prove the guarantees

- [x] Two tools under one identifier print one person row — `cli/tests/e2e/telemetry-person-mapping.e2e.test.ts:106-119`, with the "why this already holds" comment at `:102-104`
- [x] A second machine's identifier prints unresolved before linking and merges into one row after — `cli/tests/e2e/telemetry-person-mapping.e2e.test.ts:130-149`
- [x] Unlinking restores the two rows — `cli/tests/e2e/telemetry-person-mapping.e2e.test.ts:151-163`
- [x] The person rows' totals sum to the period total the report prints — `cli/tests/e2e/telemetry-person-mapping.e2e.test.ts:180-182`; mutation-verified against a rebuilt bundle (dropping records in `addToPersonGroup` gives `expected 4 to be 5`). Reconciles `requests` only at e2e level; money and input tokens are covered by the unit test
- [x] Two unmapped identifiers never collapse into one row — `cli/tests/e2e/telemetry-person-mapping.e2e.test.ts:183-186`
- [x] Identity status lists the mapped identities without a report having been produced — `cli/tests/e2e/telemetry-person-mapping.e2e.test.ts:189-199`
- [x] A mapping that does not parse costs the resolution and not one figure, and the caveat says so — `cli/tests/e2e/telemetry-person-mapping.e2e.test.ts:201-224`
- [x] A mapping placed where a repository could set it has no effect on the report — behaviour independently confirmed against the built CLI; the asserting test at `cli/tests/e2e/telemetry-person-mapping.e2e.test.ts:226-247` passes on an empty report and proves nothing — finding 3
- [x] The whole file passes with no AI tool binary on `PATH` — `cli/tests/e2e/helpers.ts:143` (`pathWithoutAidd()`), stated at `cli/tests/e2e/telemetry-person-mapping.e2e.test.ts:20-24`; full suite green, 282 files / 2995 tests

## Findings

| Sev | Kind | Phase | Location | Issue | Fix | Outcome |
| --- | ---- | ----- | -------- | ----- | --- | --- |
| 🔴 | conform | 3 | `plugins/aidd-telemetry/skills/01-cost/actions/03-report.md:79` | "`cost_report_version` is `3` today … Anything else on the `--json` path: stop, rather than guessing which field means what." The CLI now emits `4` (`cli/src/domain/models/cost-report-envelope.ts:31`, fixture `cli/tests/fixtures/cli-owns-read/expected-envelope.json:2`). An agent following this skill halts on every `--json` report and shows no figure at all — a working surface broken by this diff. `aidd_docs/product/cost-report-contract.md:112` was updated; the skill that consumes it was not. | Change the pinned version to `4` in step 3, and note that `by_person` and `read.person_mapping_unreadable` are what the bump added. | fixed — pinned version corrected to 4, and the bump's own fields (`by_person`, `person_mapping_unreadable`) named in the same step. |
| 🟡 | fit | 3 | `plugins/aidd-telemetry/skills/01-cost/SKILL.md:3,39,41-44`; `actions/03-report.md:13,73,108`; `plugins/aidd-telemetry/CATALOG.md:51` | The plugin still declares per-person "none - unanswerable", tells the agent to "stop before running the script" for a per-person question, omits `person` from its `--axis <total\|day\|step\|model\|tool\|project>` list, and its own description says "Not for a per-person figure" — so the skill will not even trigger. The capability this issue delivers is unreachable through the plugin, and the user-facing text now asserts something false. The spec's "a report line naming a person" is satisfied only by driving the raw CLI. | Replace the "unanswerable" row with a `person` axis row, delete the "Per person cannot be answered today" paragraph and the two Test-table rows that pin it, add `person` to the axis list, and drop "not for a per-person figure" from both descriptions. | fixed — `person` axis added throughout (SKILL.md, 03-report.md, CATALOG.md regenerated), the "unanswerable" text and description exclusion removed. |
| 🟡 | code | 4 | `cli/tests/e2e/telemetry-person-mapping.e2e.test.ts:226-247` | The test that proves a repository cannot supply the mapping is vacuous. `AIDD_USER_CONFIG_DIR` also relocates the telemetry sink (`cli/src/infrastructure/adapters/telemetry-sink-adapter.ts:103-105`), so pointing it at the decoy leaves the report with zero records — verified by running the built CLI: `requests 0 []`. `by_person.every(...)` on `[]` is trivially true. Confirmed decorative: mutating `resolveAiddConfigDir` to honour the variable leaves this test green (the unit path test at `person-mapping-location.unit.test.ts:78` is what actually kills it). The repo already codifies the opposite convention at `cli/tests/e2e/telemetry-cost-skill-commands.e2e.test.ts:77` ("the fixture is not vacuous, so this pin cannot pass on emptiness"). | Seed the sink under `decoyDir/telemetry` as well as the mapping, then assert `envelope.totals.requests` is 2 before asserting no row is `mapped` — so the assertion cannot pass on emptiness. | fixed — sink now seeded under `decoyDir/telemetry`; `envelope.totals.requests` asserted `2` before the no-`mapped` assertion. Mutation-verified: mutating `resolveAiddConfigDir` to honour `AIDD_USER_CONFIG_DIR` now fails this test. |
| 🟡 | rot | - | `cli/scripts/check-bundle-size.mjs:9-12` | The budget moved 560 → 590 (`cli/package.json:47`) but the comment recording why still explains the old number: "560 was set when measurement across five tools took the bundle to 500.8 KB". The script's own rule is that a raise is deliberate and "the raise is what a reviewer sees"; the record a reviewer sees is now stale. | Rewrite the comment to record the 590 raise and the 567.7 KB measurement that forced it, the way the 560 raise is recorded. | fixed — comment now records the 590 raise and the 567.7 KB measurement that forced it, alongside the original 560/500.8 KB record. |
| 🟡 | rot | 1 | `cli/tests/helpers/ports/in-memory-person-mapping-store.ts:32-56` | The double reimplements `PersonMappingAdapter.link`/`unlink` line for line — the same `findIndex`, the same chained `index === -1` ternaries, the same `identities.includes` guard, the same `validatePersonMapping(next)` (`cli/src/infrastructure/adapters/person-mapping-adapter.ts:116-140`). Two copies of the merge rule with no shared source: change the adapter's rule and every use-case test keeps passing against the old one. | Move the entry-merge rule into `cli/src/domain/models/person-mapping.ts` as pure `linkIdentity(mapping, personId, identity)` / `unlinkIdentity(mapping, identity)`, and have both the adapter and the double call it, keeping only persistence in the adapter. | skipped — not among the items this repair pass was commissioned to fix; the dedupe extraction (`linkIdentity`/`unlinkIdentity`) is a real cleanup but out of this task's scope. |
| 🟡 | code | 2 | `cli/tests/domain/models/cost-report-person.unit.test.ts:234-266` | The one test pinning `withPersonBackfill` never builds the scenario it names. Its comment says "the survivor `mergeBilledRequestGroup` picks by `cost_usd` is exactly the export record", but both records inherit `provenance: "local-read"` from `BASE` (`:26`) — the cost-bearing one is never marked `export`. The export/local-read reconciliation the production comment at `cost-report.ts:1169-1181` justifies itself with is untested. | Add `provenance: "export"` to the cost-bearing record in that test. | fixed — the cost-bearing record in the `withPersonBackfill` test now carries `provenance: "export"`. |
| 🟡 | rot | - | `cli/src/domain/models/telemetry-sink-record.ts:34-35,80-82` vs `cli/src/domain/models/cost-report.ts:1182-1198` | The record contract states "no identity of any kind on an export-provenance record" and that `person_id` is "never set on an export-provenance record". After `withPersonBackfill`, an in-memory reconciled record can carry `provenance: "export"` and `person_id` together. Nothing derives the person from an export attribute — it comes from the local-read sibling of the same billed call, so the spec's non-goal ("attaching a person to an export-provenance record") is not violated in substance. But the two files now state opposite things with no cross-reference, and the next reader of the record contract will believe the stronger claim. | Scope the invariant in `telemetry-sink-record.ts` to stored lines, and point at `withPersonBackfill` as the one read-time reconciliation that pairs the two on an in-memory record. | fixed — `telemetry-sink-record.ts`'s invariant is now scoped to stored lines, and cross-references `withPersonBackfill` as the one read-time reconciliation that pairs the two. |
| 🟢 | code | - | `cli/src/domain/models/cost-report.ts:1187-1189` | `withPersonBackfill` resolves several person-bearing siblings with `pickDeterministically` — a JSON-sort tiebreak — where `validatePersonMapping` refuses the very same ambiguity by name one layer down. Not reachable today (`collapseSupersededTurns` runs first at `:1242` and reduces each local-read turn to one record, and `billedRequestKey` at `:1129-1133` is `(tool, vendor_id, billed_request_id)` with the field documented unique per billed call), so no wrong figure ships — but the two layers state opposite policies on the same question. | State in the doc comment why two donors carrying different `person_id` cannot occur, or return `base` untouched when the donors disagree rather than sorting between them. | skipped — not among the items this repair pass was commissioned to fix. |
| 🟢 | code | 3 | `cli/src/infrastructure/adapters/person-mapping-adapter.ts:117-127` | `link` branches on `index === -1` three times across two chained ternaries, and `updatedEntry` is a no-op alias of `entry` in the create branch. The same shape is copied into the in-memory double. | One early return for the create branch, then the update branch, so each path reads once. | skipped — not among the items this repair pass was commissioned to fix. |
| 🟢 | rot | 3 | `cli/src/application/display/cost-report-artefact.ts:136-138`; `cli/src/domain/models/cost-report-envelope.ts:133-137` | An ambiguous mapping is read and parsed fine, then refused by `validatePersonMapping` (`person-mapping-adapter.ts:112`), and surfaces through the same `personMappingUnreadable` flag as a damaged file — printed as "the person mapping could not be read". `aidd_docs/product/cost-report-contract.md:314-317` defines the flag as "a mapping file existed but could not be read back", which is not what happened. The port doc at `person-mapping-store.ts:33-34` already names the three causes; the caveat text does not. | Widen the caveat wording to "could not be used" (or name the ambiguity case), and align the contract doc's definition with it. | fixed — caveat wording widened to "could not be used" in the artefact, the envelope's doc comment, `CostReportInput`/`CostReport`'s doc comments, and the contract doc's definition; both tests asserting the old wording updated. |
| 🟢 | code | 3 | `cli/tests/application/display/cost-report-artefact.unit.test.ts:64-74` | Named "prints one row per person with the identities behind it, mapped rows first", but the envelope it builds has exactly one row, so the ordering half of its own name is never exercised. Ordering is covered at `cli/tests/domain/models/cost-report-person.unit.test.ts:169`, not here. | Add an unresolved record to the fixture and assert the mapped label appears above it, or drop "mapped rows first" from the name. | skipped — not among the items this repair pass was commissioned to fix. |
| 🟢 | code | 2 | `cli/tests/domain/models/cost-report-person.unit.test.ts:147-167` | The money-and-tokens reconciliation sums `costMicroUsd` and `inputTokens` only. `outputTokens`, `cacheReadTokens` and `cacheCreationTokens` are set nowhere in the fixture, so a per-row accumulator that dropped one of them would not be caught here. | Set all four token counters on the fixture records and sum all four against `built.totals`. | fixed — all four token counters (`input_tokens`, `output_tokens`, `cache_read_tokens`, `cache_creation_tokens`) now set on the fixture and summed against `built.totals`. |

## Verification

| Metric        | Value                                             |
| ------------- | ------------------------------------------------- |
| Verified      | 100% (34/34)                                      |
| Files checked | `cli/src/domain/models/person-mapping.ts`, `cli/src/domain/models/cost-report.ts`, `cli/src/domain/models/cost-report-envelope.ts`, `cli/src/domain/models/telemetry-sink-record.ts`, `cli/src/domain/errors.ts`, `cli/src/domain/ports/person-mapping-reader.ts`, `cli/src/domain/ports/person-mapping-store.ts`, `cli/src/infrastructure/adapters/person-mapping-adapter.ts`, `cli/src/infrastructure/adapters/person-identity-adapter.ts`, `cli/src/infrastructure/home-dir.ts`, `cli/src/infrastructure/json-file.ts`, `cli/src/infrastructure/deps.ts`, `cli/src/infrastructure/errors.ts`, `cli/src/application/use-cases/telemetry/person-mapping-use-case.ts`, `cli/src/application/use-cases/telemetry/person-identity-use-case.ts`, `cli/src/application/use-cases/telemetry/report-cost-use-case.ts`, `cli/src/application/display/cost-report-artefact.ts`, `cli/src/application/display/telemetry-display.ts`, `cli/src/application/commands/telemetry.ts`, `cli/src/application/errors.ts`, `cli/package.json`, `cli/scripts/check-bundle-size.mjs`, `cli/tests/domain/models/person-mapping.unit.test.ts`, `cli/tests/domain/models/cost-report-person.unit.test.ts`, `cli/tests/domain/models/cost-report-envelope.unit.test.ts`, `cli/tests/application/display/cost-report-artefact.unit.test.ts`, `cli/tests/application/use-cases/telemetry/person-mapping-use-case.unit.test.ts`, `cli/tests/application/use-cases/telemetry/person-identity-use-case.unit.test.ts`, `cli/tests/application/use-cases/telemetry/report-cost-use-case.unit.test.ts`, `cli/tests/infrastructure/adapters/person-mapping-location.unit.test.ts`, `cli/tests/helpers/ports/in-memory-person-mapping-reader.ts`, `cli/tests/helpers/ports/in-memory-person-mapping-store.ts`, `cli/tests/e2e/telemetry-person-mapping.e2e.test.ts`, `cli/tests/e2e/telemetry-identity.e2e.test.ts`, `cli/tests/e2e/helpers.ts`, `cli/tests/fixtures/cli-owns-read/expected-envelope.json`, `aidd_docs/product/cost-report-contract.md`, `plugins/aidd-telemetry/skills/01-cost/SKILL.md`, `plugins/aidd-telemetry/skills/01-cost/actions/03-report.md`, `plugins/aidd-telemetry/CATALOG.md` |
| Unchecked     | none                                              |
| Unplanned     | `cli/package.json:47` budget 560 → 590, asked for by no phase — justified, the measured bundle is 567.7 KB and 560 would fail the build; 22.3 KB (3.9%) of headroom is tighter than the precedent raise 500 → 560, which left 59.2 KB (11.8%) over a measured 500.8 KB. **Outcome: no_change_needed — re-verified during repair: `npm run build` measures 567.66-567.72 KB across rebuilds, so 560 would fail and 590 is the correct, tighter-than-precedent budget; left as is.** `withPersonBackfill` in `cli/src/domain/models/cost-report.ts:1182-1198`, asked for by no phase — required: without it a mapped person's own billed call is reported as `none` whenever the export sibling survives, which phase 2's "summing every person row's totals equals the report's own total" would still satisfy while mislabelling the row. **Outcome: no_change_needed — re-verified during repair: the function is load-bearing (only its own test fixture was under-testing the scenario, fixed separately); left as is.** `resolveAiddConfigDir` extracted to `cli/src/infrastructure/home-dir.ts:41-46` and the JSON helpers to `cli/src/infrastructure/json-file.ts`, asked for by no phase — implied by phase 3 task 1.2's "reusing the identity adapter's own directory resolution rather than restating it". `aidd_docs/product/cost-report-contract.md`, asked for by no phase — required by the envelope bump phase 2 task 3 does ask for. |
