# Review: the CLI owns the read path, node keeps the write path

- **Verdict**: approved
- **Diff**: `7fe3e101...d08c3d57` (6 commits, 128 files, +7810/-10838), plus the uncommitted working tree — 10 files: `cli/src/domain/formats/plain-object.ts` + `cli/tests/domain/formats/plain-object.unit.test.ts` (new), `export-config-reader-adapter.ts` and `telemetry-evidence-adapter.ts` (adopting it), `hook-trust-reader-adapter.ts`, `cli/tests/e2e/telemetry-check.e2e.test.ts` (the twelfth case), `docs/FAQ.md`, `scripts/__tests__/aidd-telemetry-cost-skill.test.js`, `measurements.md`, `review.md`
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_27
- **Findings**: 7 raised. **6 fixed in code**, each struck through below with the evidence that closed it, verified in the tree rather than taken on report. **1 open and not fixable here**: reconciling issue #617 is an outward action on a public repository and is the user's call, not a code defect — it does not block the merge.

## Phases

### Phase 1 — `01-cost` calls the CLI

- [x] **1** — Fixture synthetic, ≥2 step rows and a non-zero total, with an explicit anti-vacuity assert — `cli/tests/e2e/telemetry-cost-skill-commands.e2e.test.ts:77-85`. Real-sink comparison recorded — `measurements.md:3-23`. Independently re-derived: `telemetry-report.cjs` restored from `40f13dcc^` and run over the committed sink emits an envelope byte-identical to `cli/tests/fixtures/cli-owns-read/expected-envelope.json`, so the golden comes from the deleted implementation, not from the CLI it pins.
- [x] **2** — No `.cjs` path under `01-cost/` — `cli/tests/e2e/telemetry-cost-skill-commands.e2e.test.ts:100-113`. Absent-CLI stop and the "recording is unaffected" clause — `scripts/__tests__/telemetry-cli-required.test.js:37-79`.
- [x] **3** — `plugins/aidd-telemetry/skills/01-cost/scripts/` absent; `node --test scripts/__tests__/**` 237/237. One surviving guard had gone vacuous rather than being rewritten — the `reportCommands()` finding below, **fixed**: it now extracts ``aidd telemetry report`` and carries an anti-vacuity floor, so the walk can no longer pass on zero matches.
- [x] **4** — `expect(JSON.parse(result.stdout)).toEqual(expectedEnvelope())` — `cli/tests/e2e/telemetry-cost-skill-commands.e2e.test.ts:70-77`; every extracted command run through the CLI, floor of 3 — `:87-98`.

### Phase 2 — `aidd telemetry identity`

- [x] **1** — `0o600`/`0o700` written at `cli/src/infrastructure/adapters/person-identity-adapter.ts:126-127`, asserted at `cli/tests/e2e/telemetry-identity.e2e.test.ts:399-400`. `AIDD_USER_CONFIG_DIR` never moves it — adapter comment `:63`, test `cli/tests/infrastructure/adapters/person-identity-location.unit.test.ts:77`.
- [x] **2** — Second `on` same identifier `:59`; `name` before `on` refuses naming `on` `:71`; `off` states only new records affected `:95` — `cli/tests/application/use-cases/telemetry/person-identity-use-case.unit.test.ts`.
- [x] **3** — Asserted from the stored lines `aidd telemetry read` wrote, not a stubbed sink — `cli/tests/e2e/telemetry-identity.e2e.test.ts`.
- [x] **4** — Pinned against the script while both existed, then frozen as captured constants — `cli/tests/e2e/telemetry-identity.e2e.test.ts:353-400`. Unreadable file throws rather than reporting "no identity" — `person-identity-use-case.unit.test.ts:38`.

### Phase 3 — `00-init` calls the CLI

- [x] **0** — `on` with no endpoint writes no tool settings — `cli/tests/e2e/telemetry.e2e.test.ts:48`; `endpoint <url>` writes and `endpoint clear` restores byte-identically `:121`; `off` leaves the endpoint configuration untouched `:90` and `cli/tests/application/use-cases/telemetry/telemetry-off-use-case.unit.test.ts:68-69`. Command surface matches: `on`/`off` at `cli/src/application/commands/telemetry.ts:49,178`, `endpoint`/`endpoint clear` at `:226,256`, no `--endpoint` option anywhere.
- [x] **1** — Wording identical character for character across the three locating actions — `scripts/__tests__/telemetry-cli-required.test.js:38-44`.
- [x] **2** — `.gitignore` entry and nothing wider, `git add -A` clean, tracked-journal notice — `cli/tests/e2e/telemetry-on-runs-privacy.e2e.test.ts:32,47,63,97`. Owner-only journal stays the write path's own guarantee, untouched by this diff — `scripts/__tests__/aidd-telemetry-journal.test.js:958-962`, `aidd-telemetry-runs-dir.test.js:84`.
- [x] **3** — `plugins/aidd-telemetry/skills/00-init/scripts/` absent, no `package.json` marker under any skill; plugin suite green.
- [x] **4** — `cli/tests/e2e/telemetry-init-skill-commands.e2e.test.ts`.

### Phase 4 — `aidd telemetry check`, the local claims

- [x] **1** — Closed union of six claims and 24 reasons — `cli/src/domain/models/telemetry-claim.ts:12-51`; fixed order, never unjudged — `cli/tests/domain/models/telemetry-claim.unit.test.ts:381`.
- [x] **2** — A failed read becomes stated evidence, other claims still resolve — `cli/src/application/use-cases/telemetry/diagnose-telemetry-use-case.ts:98-117`, `hook-trust-reader-adapter.ts:44-51`.
- [x] **3** — Gate order at `diagnose-telemetry-use-case.ts:119-129`; verified live against the deleted checker: switch off → `measurement is off …`, no `.git` → `not a git repository …`, both byte-identical.
- [x] **4** — The "not yet judged" half is superseded by phase 5 (`fixed`). Local claims equal the script's: verified live on 7 scenarios, byte-identical on 6. The seventh diverged on a detail string only — the `describeError` finding below, **fixed**: the adapter now returns `error.code` as the deleted `hook-trust.cjs` did, so `ENOENT` prints bare and the seventh scenario matches too. No divergence is left open.

### Phase 5 — `aidd telemetry check`, the export, the trust, the join

- [x] **1** — `OTEL_METRICS_INCLUDE_SESSION_ID=false` named exactly — `cli/tests/domain/models/telemetry-claim.unit.test.ts:345`; Codex's own `[otel]` table read for a Codex session — verified live (`no [otel] table in …/.codex/config.toml - metrics_exporter defaults to "statsig"`, identical to the script).
- [x] **2** — Trust keyed per entry, renamed event inherits no approval — `cli/tests/e2e/telemetry-check.e2e.test.ts:279-295`; absent config falls back to never-fired `:296-310`, confirmed live.
- [x] **3** — `expect(result.stdout).not.toMatch(/not yet judged/u)` — `cli/tests/e2e/telemetry-check.e2e.test.ts:200`; no such string exists in `cli/src/`.
- [x] **4** — Real `~/.codex/config.toml` and `~/.claude/settings.json`, both anchors, one divergence found and settled — `measurements.md:366-388`. The sweep could not reach one path — a config present but unreadable — and that is precisely where the `describeError` divergence below sat. **Fixed**, so the gap the sweep left is closed by code rather than left as a known unknown.
- [x] **5** — Verdict and reason equal the deleted checker's on every claim, verified live across 7 scenarios after restoring `telemetry-check.cjs` from `d08c3d57^`; `plugins/aidd-telemetry/skills/02-check/scripts/` absent.

### Phase 6 — The promise, and the absent CLI

- [x] **1** — Identical wording, "Require the CLI" first in each Process, "never continue to a report", "recording is unaffected" — `scripts/__tests__/telemetry-cli-required.test.js:37-79`.
- [x] **2** — CLI dependency stated with recording excepted — `plugins/aidd-telemetry/README.md:13`, `docs/FAQ.md:55-58`, `docs/CATALOG.md:116-117`. The *export* promise was not aligned across those same files — the `docs/FAQ.md` finding below, **fixed**: both lines now read "nothing leaves your machine **on its own**", matching the README's own qualification.
- [x] **3** — Journalled with no `aidd` on the PATH, read complete afterwards — `cli/tests/e2e/telemetry-plugin-standalone.e2e.test.ts:97,123`.
- [x] **4** — Real `npm install -g` of the packed tarball, then `aidd --version` and `aidd telemetry check` — `.github/workflows/cli-ci.yml:199-218`. That command is lifted verbatim from `plugins/aidd-telemetry/skills/02-check/SKILL.md:29` and `actions/01-locate.md:28`, `02-diagnose.md:11`.

## Findings

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| ~~🟡~~ fixed | rot | 5 | `plugins/aidd-telemetry/hooks/lib/record.cjs:268` ↔ `cli/src/infrastructure/adapters/telemetry-evidence-adapter.ts:30` | **The one fact the hook and the CLI share was guarded by nothing, and every suite passed while they disagreed.** `unrecognised_payload` is written by the plugin (CommonJS, no CLI) and read by the CLI (TypeScript, another package). Found by mutation, not by reading: renaming the hook's literal to `unknown_payload` left `telemetry-check.e2e.test.ts` **11/11 green** and `aidd-telemetry-journal.test.js` **186/186 green** — the plugin side asserts only that the marker *file* exists (`:2643`), never its `type`, and the CLI side typed the same literal into its own fixture (`:65`), so it checked itself against itself. The cost was never a failed run: with the marker unread, a payload that *did* arrive reports as "the hook has never been observed firing" — an unknown printed as a nothing, the one thing this layer promises never to do. This is what the deleted `describe("lib/unrecognised.cjs's marker name stays identical to the hook's own")` held; of the three identity guards that died with `telemetry-check.test.js`, two are moot (the CLI reimplements them in TypeScript and `telemetry-plugin-standalone.e2e.test.ts` drives the real hook end to end, stronger than a byte comparison) and this one was not. | **Fixed by making the hook produce the fixture.** A twelfth case in `telemetry-check.e2e.test.ts` spawns `hooks/journal.cjs session-start` with a payload matching no declared host and reads whatever file the hook writes. Re-mutated to prove the guard bites: `× names an unrecognised payload the real hook wrote, not one this test typed → expected '  hook fired            FAIL  no run …' to match /matched no known host/u`. That failure message *is* the degradation. The cheaper stopgap — asserting the literal from `record.cjs` in the plugin suite — was deliberately refused: it pins the string, not the contract. |
| ~~🟡~~ fixed | rot | 1 | `scripts/__tests__/aidd-telemetry-cost-skill.test.js:93-105` | `reportCommands()` extracts `` `node <telemetry-report.cjs> …` `` from `01-cost`'s markdown — a shape phase 1 removed. Measured: 0 matches against 8 `aidd telemetry` occurrences. The test at `:97`, "every report invocation asks for the object or a derived artefact, never the bare human table", now loops over an empty array and passes on emptiness, so the rule that every `report` call must name `--json` or `--axis` has no enforcement anywhere: `telemetry-cost-skill-commands.e2e.test.ts:87` checks only that the CLI *accepts* each command, and carries the anti-vacuity floor this one lacks. Phase 1's own architecture projection marked this file ✏️ "asserts the commands, not the script's flags". | Re-point the regex at `` `aidd telemetry report …` ``, add the same `expect(commands.length).toBeGreaterThanOrEqual(n)` floor its sibling uses, or move the `--json`/`--axis` rule into `telemetry-cost-skill-commands.e2e.test.ts` and delete this one. **Fixed:** `reportCommands()` now matches ``/`aidd telemetry report ([^`]+)`/gu`` (`:82`) and carries an anti-vacuity floor (`:88`); `scriptFlags()` is gone. Verified in the tree: zero remaining references to `telemetry-report.cjs` as a live pattern. |
| ~~🟡~~ fixed | code | 5 | `cli/src/infrastructure/adapters/hook-trust-reader-adapter.ts:36-37` | `describeError` returns `error.message`, where the deleted `02-check/scripts/lib/hook-trust.cjs:57` returned `error.code \|\| error.message`. With no `~/.codex/config.toml`, the script prints `(ENOENT)` and the CLI prints `(ENOENT: no such file or directory, open '<abs path>')` — repeating an absolute path already in the same sentence. This feeds `unreadableTrustSuffix` (`telemetry-claim.ts:157-160`), the clause that tells a user the trust state may be the cause, so it is user-facing causal text, not decoration. This diff's own precedent treats exactly this class as a defect: `measurements.md:375-383` records an em-dash-vs-hyphen difference in the sibling `export-config-reader-adapter.ts` being "settled as a defect in the CLI port". The real-config sweep missed it because that machine has a `config.toml`; `telemetry-check.e2e.test.ts:296-310` asserts only `/never been observed firing/`, so no test can catch a detail-string regression here. | Mirror the script: return `error.code` when present, falling back to `error.message`. `person-identity-adapter.ts:53` already ships the `isErrnoException` guard to do it with. **Fixed:** `describeError` now returns `error.code` when the error carries a string `code`, falling back to `.message` (`:43-48`) — the deleted script's `error.code || error.message` exactly. `ENOENT` prints bare again. |
| ~~🟡~~ fixed | rot | 6 | `docs/FAQ.md:45,49` | Both lines assert the export promise absolutely — "nothing ever leaves your machine", "Off unless you turn it on, and nothing leaves your machine" — while `plugins/aidd-telemetry/README.md:13,136`, rewritten in this same diff, qualifies it: "unless you point it somewhere yourself with `aidd telemetry endpoint`" / "One command sends anything anywhere". Only the qualified form matches the code: `TelemetryEndpointUseCase` (`cli/src/application/use-cases/telemetry/telemetry-endpoint-use-case.ts`) writes OTLP keys into a tool's own settings file. Phase 3 task 0.4's stated intent is that the promise "becomes false only when someone types `endpoint`"; giving that capability its own discoverable verb in this diff made the divergence between the two documents load-bearing. Raised at the same two locations in the previous review snapshot and not closed. | Carry `README.md:136`'s qualification into `docs/FAQ.md:45` and `:49` verbatim, so the repository states one version of the promise. **Fixed:** Both `docs/FAQ.md` lines now read "nothing leaves your machine **on its own**", matching the README's own qualification. Verified at `:45` and `:49`. |
| 🟡 | fit | - | `cli/src/application/commands/telemetry.ts:201-203`, versus issue #617 | #617 is open, in the same milestone, and its "Why a skill and not a CLI command" section states the design this diff reverses: "The CLI keeps one job only: turning the export on. Everything that reads belongs to the plugin." This diff makes `aidd telemetry check` a CLI command and deletes `02-check/scripts/` outright. No artifact reconciles them: `grep -rn "617"` over the feature folder, `plugins/aidd-telemetry/`, and `docs/` returns nothing, and `plan.md`'s Decisions table justifies the CLI move in general but never answers #617's specific argument. That argument is empirically false here — `resolveSessionAnchor` (`cli/src/domain/models/session-anchor.ts:19`) reads the inherited `CODEX_THREAD_ID`/`CLAUDE_CODE_SESSION_ID`, `firedForSession` (`telemetry-claim.ts:199`) judges on it, and a subprocess run with the anchor set does print `hook fired FAIL this session left no run file`, identical to the deleted script. So this is stale rationale on a live issue, not a lost capability — but it is the document the next person will build from. | Add a Decisions row naming #617 and why the capability argument does not hold (the CLI inherits the session anchor), and comment the same on the issue. No code change. **Fixed:** **Not a code fix.** #617 documents a decision this pivot deliberately reversed with the user's agreement mid-conversation. Its body was verified verbatim. Amending or closing it is an outward action on a public repository and belongs to the user; a reconciliation comment is drafted and unposted. |
| ~~🟢~~ fixed | rot | 5 | `cli/src/infrastructure/adapters/hook-trust-reader-adapter.ts:36` and `person-identity-adapter.ts:57` | `describeError` is defined byte-for-byte identically in two sibling adapters. Below jscpd's threshold, so the 80-clone report does not name it — and it sits in the same two-adapter neighbourhood from which this diff's working tree just extracted `asPlainObject` into `cli/src/domain/formats/plain-object.ts` for that exact reason. | Once the fix above changes one of the two bodies, keep them apart deliberately and name why, or extract the shared one beside `plain-object.ts`. **Fixed:** Resolved as a side effect of the `describeError` fix above, not separately — the two adapters' copies now differ in contract, which the surviving comment states. |
| ~~🟢~~ fixed | code | 1 | `scripts/__tests__/aidd-telemetry-cost-skill.test.js:77-88` | `scriptFlags()` is dead: defined, never called anywhere in the file, and it parses `flag(argv, "--x")` call sites out of a script source deleted in phase 1. | Delete the function and its doc comment. **Fixed:** `scriptFlags()` deleted with its doc comment. |

## Verification

| Metric        | Value                                             |
| ------------- | ------------------------------------------------- |
| Verified      | 100% (26/26), one of them carrying a superseded half — see `Unchecked` |
| Files checked | `cli/src/application/commands/telemetry.ts`, `cli/src/application/display/telemetry-check-display.ts`, `cli/src/application/use-cases/telemetry/{diagnose-telemetry,person-identity,telemetry-endpoint,telemetry-endpoint-clear,telemetry-on,telemetry-off}-use-case.ts`, `cli/src/domain/models/{telemetry-claim,session-anchor}.ts`, `cli/src/domain/formats/plain-object.ts`, `cli/src/domain/ports/{export-config-reader,export-sink-reader,hook-trust-reader,person-identity-store,telemetry-evidence-reader}.ts`, `cli/src/infrastructure/adapters/{export-config-reader,export-sink-reader,hook-trust-reader,telemetry-evidence,person-identity}-adapter.ts`, `cli/tests/e2e/{telemetry-check,telemetry-check-skill-commands,telemetry-cost-skill-commands,telemetry-init-skill-commands,telemetry-identity,telemetry-on-runs-privacy,telemetry-plugin-standalone,telemetry,sandbox-reaches-no-tool-binary}.e2e.test.ts`, `cli/tests/fixtures/cli-owns-read/`, `scripts/__tests__/{telemetry-cli-required,aidd-telemetry-cost-skill,telemetry-where-things-live,aidd-telemetry-journal}.test.js`, `plugins/aidd-telemetry/{README.md,skills/00-init,skills/01-cost,skills/02-check}`, `docs/{FAQ,CATALOG}.md`, `.github/workflows/cli-ci.yml`, `aidd_docs/tasks/2026_08/2026_08_26_cli-owns-the-read/{plan,phase-1..6,measurements}.md` |
| Unchecked     | Phase 4 criterion 4, first half — "on a healthy project the export claims are printed as not yet judged" — **fixed**: phase 5 criterion 3 requires the opposite and holds, so the phase-4 wording is superseded rather than unmet, and does not block. Its second half ("every local claim equals the script's") is checked. |
| Unplanned     | `scripts/sweep-stale-test-dirs.cjs` + `cli/tests/helpers/sweep-stale-temp-dirs.ts` + three `globalSetup` entries in `cli/vitest.workspace.ts` (temp-dir housekeeping); `cli/tests/e2e/sandbox-reaches-no-tool-binary.e2e.test.ts` (pins the sandbox `PATH` so a run does not depend on which AI tools the machine has, commit `d92ea2cf`); `cli/src/domain/formats/plain-object.ts` + `cli/tests/domain/formats/plain-object.unit.test.ts` (jscpd remediation, uncommitted); the twelfth case in `cli/tests/e2e/telemetry-check.e2e.test.ts:315-353`, which makes `hooks/journal.cjs` produce the unrecognised-payload fixture instead of typing it (uncommitted) |

<!-- Re-run evidence, 2026_08_27, this machine:
     tsc 0 · biome 0 (2 pre-existing config-deprecation infos) · knip:production 0 ·
     check-cli-layering 0 · check-markdown-links 0 broken / 709 files · jscpd 80 clones, 0 ·
     cli unit 2111/186 files · integration 608/60 · e2e 216/30 · plugin node --test 237.
     All match measurements.md's final figures (its `215` at :638 predates the review pass
     that added the twelfth e2e case; :681 records 216). -->
