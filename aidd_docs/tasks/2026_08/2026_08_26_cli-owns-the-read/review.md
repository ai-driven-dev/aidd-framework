# Review: `00-init` calls the CLI (phase 3, tasks 0–4)

- **Verdict**: changes-requested
- **Diff**: `40f13dcc...working-tree` (uncommitted, plus untracked source/test files; no commit, no PR)
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_26
- **Findings**: 1 critical, 5 warning, 13 minor (2 carried unresolved from phase 2)

## Phases

### Phase 2 — `aidd telemetry identity` (cleared previously, not re-reviewed)

- [x] **Tasks 1–4** — Approved in the previous snapshot of this file; carried unchanged. Two of its four minor findings are **fixed** in this candidate: `measurements.md:91` now lists `status`/`on`/`name` only, so the section no longer contradicts itself; `IdentityWriteError` now takes an `action` argument and `person-identity-adapter.ts:110` passes `"remove"`, so a failed withdrawal no longer reports a failed write. The `try`/`catch`-inside-a-use-case conform note and the tmpdir leak stay open, below.

### Phase 3 — `00-init` calls the CLI

- [x] **Task 0** — `aidd telemetry on` succeeds with no endpoint and writes no tool's settings; `endpoint <url>` writes them and `endpoint clear` removes them; `off` leaves an endpoint configuration untouched. — `telemetry-on-use-case.ts:39` (switch + `protectRunsDir` only, no manifest, no tool settings); `telemetry.e2e.test.ts:48` asserts `LOCAL_SETTINGS_PATH` absent after `on`; `:100` arms then `off` then re-reads the settings file byte-for-byte identical and proves `endpoint clear` can still find it; `telemetry-endpoint-clear-use-case.ts` is the ported remover. `off` unit `:69` pins the same at the use-case level. `UnconfirmedRemoteTelemetryEndpointError` is deleted rather than weakened: the endpoint can no longer be inherited from the repository-carried switch file at all (`telemetry-endpoint-use-case.ts:80` comment; the command's `<url>` is required), so the guard's own precondition is gone — a non-loopback destination now warns (`:130`) on a value the person typed, which is what the deleted error already permitted.
- [x] **Task 1** — No file under `00-init/` names a `.cjs` path, and its absent-CLI wording is identical to `01-cost`'s. — filesystem walk asserts the first (`telemetry-init-skill-commands.e2e.test.ts:93`); independently re-run here across all six markdown files, zero hits. The second verified by extracting the span "No output, or a command that is not found" … "cost nothing." from both files: **561 characters, byte-identical**, `diff` clean. Guarded going forward by the containment test at `:104`. See the `conform` finding below on the *content* of that shared copy — the criterion is met, the copy fits 01-cost's vocabulary and not 00-init's.
- [ ] **Task 2** — After `aidd telemetry on`, `aidd_docs/runs/` is git-ignored, `git add -A` succeeds, and the journal is owner-only. — **partial**. The first two legs hold: `telemetry-on-runs-privacy.e2e.test.ts:32` (`.gitignore` gets exactly `aidd_docs/runs/`, nothing wider), `:137` (`git add -A` stages the `.gitignore` change and nothing under `aidd_docs/runs/`). The third does not: `measurements.md` attributes owner-only to `aidd-telemetry-journal.test.js:959-962` while stating in the same table that `aidd telemetry on` "never touches" the journal's modes. The mode claim is true of the hook's own write path; the causal link this criterion asserts — *after `aidd telemetry on`* — is asserted nowhere. The one job that ran the real chain end to end on the platform the phase's Test Scope singles out ("the measured Windows rule") is `cli-ci.yml`'s Windows job, which this candidate breaks at its first step. Tagged `fix`.
- [x] **Task 3** — `00-init/scripts/` no longer exists and the suite is green without it. — `ls` confirms the directory is gone (4 files, 278 lines, matching the projection); the `package.json` marker was already absent at `40f13dcc` (`git show` errors), so `measurements.md`'s account of `ec15a80f` holds. `aidd-telemetry-identity.test.js` deleted as named. Suite green per the caller's own run (335 plugin tests), and the 365 → 335 drop reconciles exactly. What did **not** survive the deletion is recorded as a `rot` finding below — the criterion is about the suite being green, and it is.
- [x] **Task 4** — Every command extracted from `00-init` is accepted by the CLI. — re-ran the extraction independently: 7 commands (`on`, `identity on`, `identity`, `identity status`, `identity name "<value>"`, `off`, `identity off`), all reachable in `telemetry.ts` (`:47`, `:246`, `:257`, `:270`, `:283`, `:180`). The ordering function is sound — `identity name "<value>"` and `identity status` both rank 1, so neither can run before `identity on` ranks 0 — and the vacuous-pass guard at `:78` is present. `03-verify.md` names no `aidd telemetry` command at all (it runs `ls aidd_docs/runs/*.jsonl`), so extracting nothing from it is correct, not a subset: the walk does visit it, and the `.cjs` guard at `:93` covers it too.

## Findings

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| 🔴 | rot | 3 | `.github/workflows/cli-ci.yml:173` | **The repository's own CI executes a file this candidate deletes.** The Windows job's first chain step runs `node plugins/aidd-telemetry/skills/00-init/scripts/telemetry-switch.cjs on`; `ls` confirms that path no longer exists. The job aborts there, so the three steps after it never run — including `Chain - the journal stays private and git add -A still works`, the only place `git add -A` is exercised on Windows at all. Lines `:187-188` are worse: they run `telemetry-report.cjs read|report`, deleted back in **phase 1** at `40f13dcc`, so that job has been red since before this candidate and no review caught it. No local check reaches a workflow file — `check-markdown-links.test.js` walks `[..](..)` links only, `plugin-install-shape.test.js` discovers scripts that exist rather than references to scripts that do not. `phase-6.md` does project this file, but leaving CI red for three phases is not a plan. | Two sizes. **Phase-3-sized:** seed `.aidd/config.json` directly at `:173`, exactly the move `telemetry-plugin-standalone.e2e.test.ts:50` just made for the same reason (that environment has no CLI either) — one line, no job restructure. **Phase-6-sized:** move `cd cli && pnpm install` + `pnpm build` above the chain and call the built CLI. `:187-188` have no cheap equivalent: they need the built CLI or deletion. Then close the class: extract every `.cjs` path named in any plugin markdown **and** any workflow file, and assert the file exists — `plugin-install-shape.test.js` already walks `skills/*/scripts/`, so this is the same walk inverted. |
| 🟡 | functional | 3 | `phase-3.md` task 2 | Criterion 2's "and the journal is owner-only" is unproven **as a consequence of `aidd telemetry on`**. The cited evidence (`aidd-telemetry-journal.test.js:959-962`) proves the hook writes `0600`/`0700`; `measurements.md` states in the same table that `on` never touches those modes. Two true statements that do not compose into the criterion. | Either restate the criterion to name the hook as its subject (it is the hook's guarantee, and `on` is only the thing that makes the hook run), or add one e2e that turns `on`, replays the journal hook, and stats the result. Fixing the Windows job restores the platform where this actually mattered. |
| 🟡 | rot | 3 | `cli/src/application/display/telemetry-display.ts:125,127,144,146` | **A consent disclosure died with the deleted suite.** `aidd-telemetry-identity.test.js` asserted `/Attaches to:/u` and `/Never attaches to:/u` on `on`'s stdout, and `/already stored keep the identifier/u` on `off`'s. Grepping `cli/tests/` for those literals returns **nothing**. The journey e2e (`telemetry-identity.e2e.test.ts:76`) asserts only `/on, [0-9a-f-]{36}/` for `on` and `/new records/iu` for `off`. All four disclosure lines could be deleted from the source and every suite would stay green — while `04-identify.md`'s Test table requires "the run's output says what it does and does not attach to" and `05-forget.md`'s requires "the message names what stays and what starts fresh". This is the same shape as phase 1's escaped loss: the assertion lived with the script, and went with it. | Extend the journey test: after `identity on`, assert the two disclosure lines; after `identity off`, assert "already stored keep the identifier" and "mints a fresh identifier". Four `toContain` calls in a test that already runs both verbs. |
| 🟡 | rot | 3 | `cli/tests/e2e/telemetry-identity.e2e.test.ts:208` | **A second claim died with the deleted suite: the repository cannot force an identity.** `aidd-telemetry-identity.test.js`'s last describe block seeded a project's own `.aidd/config.json` with `{ telemetry: { enabled: true, person_id: "forced-by-the-repo" } }` and asserted `status` still read `off - records carry no person`. The surviving CLI test uses the same `"forced-by-the-repo"` literal but a **different vector**: it seeds an `identity.json` under `AIDD_USER_CONFIG_DIR`. `.aidd/config.json` is never seeded with an identity-shaped key anywhere in `cli/tests/` (grepped). `00-init/SKILL.md`'s transversal rule names both sources — "never reads `.aidd/config.json` **or** `AIDD_USER_CONFIG_DIR`" — and only one half is now guarded. Not exploitable today (`person-identity-adapter.ts` opens only the user profile), but `read-local-cost-use-case` already reads both files, so a future widening would fail nothing. | Add one e2e beside `:208`: seed `.aidd/config.json` with a `person_id`, run `identity status`, assert `off` and that the literal does not appear. Six lines, and it restores the second half of the rule the skill states. |
| 🟡 | fit | - | `plugins/aidd-telemetry/README.md:24` | "Each skill checks that `aidd` answers before it does anything, and stops with the reason when it does not" is **false for `02-check`**, one of the three skills the sentence's own bullet list names two lines above. `02-check/SKILL.md:29` reads "Run only `scripts/telemetry-check.cjs`, beside this skill. […] and never the `aidd` command", and `02-check/actions/01-locate.md` locates that script rather than requiring the CLI. The rewrite states a property phase 6 is scheduled to create as though it already holds. | Scope the sentence to the two skills where it is true (`00-init`, `01-cost`), or drop it until phase 6 lands `02-check`'s locate action. |
| 🟡 | rot | 0 | `plugins/aidd-telemetry/README.md:13,126` | The front page asserts "nothing ever leaves your machine" in bold, and the Privacy section says "Sending these figures anywhere is **planned and not built**". `aidd telemetry endpoint <url>` is built, and the caller's own probe shows it writing 6 OTEL keys into `.claude/settings.local.json`. Pre-existing — `telemetry on --endpoint` did the same at `40f13dcc` — so not introduced here. But task 0.4's acceptance is explicitly *"'nothing leaves the machine' stays true for `on`, and becomes false only when someone types `endpoint`"*, and only `02-enable.md` was brought in line. Task 0 gave the capability its own discoverable verb and its own help text; the promise is now conditional and the plugin's front page still states it absolutely. `docs/FAQ.md:45` repeats it. | Qualify both README lines and the FAQ line: nothing leaves the machine unless someone runs `aidd telemetry endpoint <url>`, which is what arms a tool to export. |
| 🟢 | code | - | `.github/workflows/cli-ci.yml:181-183` | The step named `Chain - the journal stays private and git add -A still works` runs `git add -A` then `git status --porcelain >/dev/null` — the porcelain output is discarded, so the step only ever proved both commands **exit 0**. The privacy half its own name claims (the journal staying out of the index) was never asserted there, before or after this candidate. Independent of the critical finding above, which is why the job never reaches this step at all. | Pipe the porcelain through a check that no staged path is under `aidd_docs/runs/`, the way `telemetry-on-runs-privacy.e2e.test.ts:145` already does on Linux. |
| 🟢 | rot | 3 | `aidd_docs/tasks/.../measurements.md`, "Task 2" | "already pinned five of the six claims […] `git add -A` […] is now the sixth test in that same file." Both halves are wrong. The deleted suite's six tests map **1:1** onto `telemetry-on-runs-privacy.e2e.test.ts`'s tests 1–5 and 7; `git add -A` was never one of the six (it comes from the phase's acceptance table) and is the **seventh** test in a seven-test file. Same class of error phase 2's record had to be corrected for. | "All six, plus a seventh the deleted suite never had: `git add -A`." |
| 🟢 | rot | 3 | `aidd_docs/tasks/.../measurements.md`, "Net test counts" | The `Before` column is a within-worktree state (post-task-0, pre-tasks-1-4) that no command can reproduce — `40f13dcc` gives different numbers and the working tree gives the `After` ones. The arithmetic is internally consistent and the `After` figures match the caller's run, but the baseline is unfalsifiable as written. | Say the baseline is the worktree mid-phase, or drop the column and keep only the accounted-for deltas. |
| 🟢 | code | 3 | `cli/tests/e2e/telemetry-on-runs-privacy.e2e.test.ts:88` | The ported "already tracked" test drops the deleted suite's `assert.ok(fs.existsSync(trackedFile), "the file must not be removed")`. What survives asserts the warning fires and that history was not rewritten, but not that the tracked journal file is still on disk — the half of "nothing removed or rewritten" that says *removed*. | Add `expect(existsSync(join(projectDir, trackedRelative))).toBe(true)` before the `git log` assertion. |
| 🟢 | code | 2 | `cli/src/application/use-cases/telemetry/telemetry-on-use-case.ts:60` | The already-tracked notice moved from **stdout** (the deleted `journal-privacy.cjs` wrote it with `process.stdout.write`) to **stderr with a `Warning: ` prefix** (`logger.warn` → `output.ts:22`). The deleted suite asserted stdout; the new e2e asserts stderr, so the change is pinned but nowhere declared. `02-enable.md` step 4 tells the agent to "Relay what it prints about existing history" without saying which stream. Separately, no test anywhere asserts the *`.gitignore` was added* sentence itself (`:53`) — only that the file changed — although "the change is announced" is the stated reason that line exists. | Either use `info` (it is a statement of fact about the repository, not a caution about this run) or say `stderr` in `02-enable.md`. Add one assertion on the "Added `aidd_docs/runs/` to .gitignore" sentence in the first privacy test. |
| 🟢 | rot | 3 | `cli/tests/e2e/telemetry-init-skill-commands.e2e.test.ts:37` | The extraction treats every backticked span as a runnable command, so `aidd telemetry identity` (bare) enters the set from **prose** in `SKILL.md` — "and `aidd telemetry identity`'s own verbs" — not from any instruction to run it. The CLI accommodates it: `telemetry.ts:245` carries `identity.action(() => identity.help())`, while `endpoint` — the same noun-with-verbs shape — errors on a bare invocation (`missing required argument 'url'`, per the caller's probe). Two group commands, two behaviours, and the difference traces to a markdown prose habit rather than a design choice. | Extract from fenced command blocks and "Run `…`" instructions rather than every backtick; then decide `identity`-bare and `endpoint`-bare on their merits, together. |
| 🟢 | conform | 3 | `plugins/aidd-telemetry/skills/00-init/actions/01-check.md:18-27` | The verbatim reuse the criterion demands is correct and verified, and it imports `01-cost`'s vocabulary into a skill that has no report: the shared copy says "the `aidd` command is required to **answer**" and "Never continue to **a report**". `00-init` produces neither — it enables. A reader following the rule literally in `00-init` is told to stop doing something it never does. | Not a criterion failure and not a phase-3 fix. Phase 6 already projects "one shared way to require the CLI" across all three locate actions; that shared wording has to be neutral about report-versus-enable, or the third call site will inherit the same mismatch. |
| 🟢 | rot | 3 | `cli/src/infrastructure/adapters/person-identity-adapter.ts:13`, `cli/src/infrastructure/home-dir.ts:13`, `cli/tests/e2e/helpers.ts:100` | Three "mirrors the plugin's own `skills/00-init/scripts/lib/identity.cjs`, field for field and path for path" comments now point at a file this candidate deletes. They were the standing explanation of *why* the adapter's field names and path rules are what they are; the referent is gone and the contract they described now lives in the two fixture pins. | Repoint them at `cli/tests/e2e/telemetry-identity.e2e.test.ts:325` ("the on-disk format the deleted script produced"), which is what carries that contract now. |
| 🟢 | rot | 1 | `scripts/__tests__/aidd-telemetry-cost-skill.test.js:60,88` | Two comments still cite `scripts/telemetry-report.cjs:268-273` and "Every `node <telemetry-report.cjs> ...` the skill's actions write" — a file deleted in phase 1. Pre-existing, surfaced by this candidate editing the same file. | Drop or repoint while the file is already being touched. |
| 🟢 | rot | 3 | `cli/tests/domain/models/plugin-asset-translation.unit.test.ts:40-41` | `ARTEFACTS` lists `"hooks/journal.cjs"` twice. Pre-existing duplication, now the first two entries of the list since the `telemetry-switch.cjs` line above them was removed. | Delete the duplicate. |
| 🟢 | rot | 3 | `cli/tests/e2e/telemetry-plugin-standalone.e2e.test.ts:106` | "keeps whatever else the project's config already held" — the removed test asserted both an unrelated top-level key and an existing `telemetry.endpoint` survive an `on`. Not lost: `telemetry-switch.unit.test.ts:77` covers unrelated top-level keys and `telemetry-on-use-case.unit.test.ts:39` covers the endpoint. Downgraded, though, from an end-to-end proof against a real file to a pure-function one — `measurements.md` says "already prove the same claims", which overstates the level. | Note the level change in `measurements.md`, or promote one of the two into `telemetry.e2e.test.ts`, where the other `on` claims already live. |
| 🟢 | rot | - | `cli/tests/e2e/helpers.ts:50`, `scripts/__tests__/*.test.js` | Carried unresolved from phase 2: temp directories are reclaimed only by a per-test `finally` or an `after()` hook, so an interrupted run leaks its sandboxes permanently. Still open in this candidate. | A global setup sweeping stale `aidd-*` directories under `os.tmpdir()` before the first test. Track outside this phase. |
| 🟢 | conform | 2 | `cli/src/application/use-cases/telemetry/person-identity-use-case.ts:80` | Carried unresolved from phase 2: `readForWithdrawal()`'s `try`/`catch` inside a use-case, against `cli/.claude/rules/00-architecture/0-error-handling.md`. Narrow, tested, and the rule's intent honoured. Unchanged here. | Move the tolerance behind the port, or amend the rule to state the exception 20 of 104 use-case files already practise. |

## Verification

| Metric        | Value                                             |
| ------------- | ------------------------------------------------- |
| Verified      | 80% (4/5 phase-3 criteria; task 2 partial)        |
| Files checked | `.github/workflows/cli-ci.yml`, `plugins/aidd-telemetry/README.md`, `plugins/aidd-telemetry/skills/00-init/SKILL.md`, `.../actions/01-check.md`, `.../02-enable.md`, `.../03-verify.md`, `.../04-identify.md`, `.../05-forget.md`, `plugins/aidd-telemetry/skills/01-cost/actions/01-locate.md`, `plugins/aidd-telemetry/skills/02-check/SKILL.md`, `.../02-check/actions/01-locate.md`, deleted `skills/00-init/scripts/{telemetry-switch,telemetry-identity}.cjs` + `lib/{journal-privacy,identity}.cjs`, deleted `scripts/__tests__/{aidd-telemetry-identity,aidd-telemetry-switch-gitignore}.test.js`, `scripts/__tests__/aidd-telemetry-cost-skill.test.js`, `scripts/__tests__/plugin-install-shape.test.js`, `scripts/__tests__/aidd-telemetry-journal.test.js`, `scripts/__tests__/check-markdown-links.test.js`, `cli/src/application/commands/telemetry.ts`, `cli/src/application/display/telemetry-display.ts`, `cli/src/application/output.ts`, `cli/src/application/errors.ts`, `cli/src/domain/errors.ts`, `cli/src/infrastructure/errors.ts`, `cli/src/domain/models/telemetry-switch.ts`, `cli/src/domain/ports/version-control.ts`, `cli/src/infrastructure/adapters/git-adapter.ts`, `cli/src/infrastructure/adapters/person-identity-adapter.ts`, `cli/src/infrastructure/deps.ts`, `cli/src/application/use-cases/telemetry/{telemetry-on,telemetry-off,telemetry-endpoint,telemetry-endpoint-clear}-use-case.ts`, `cli/src/application/use-cases/shared/gitignore-use-case.ts`, `cli/tests/e2e/{telemetry,telemetry-identity,telemetry-init-skill-commands,telemetry-on-runs-privacy,telemetry-plugin-standalone,telemetry-lifecycle,telemetry-journal-gitignore}.e2e.test.ts`, `cli/tests/domain/models/{telemetry-switch,plugin-asset-translation}.unit.test.ts`, `cli/tests/application/use-cases/telemetry/*.unit.test.ts`, `cli/tests/application/use-cases/helpers.ts`, `docs/FAQ.md`, `aidd_docs/tasks/2026_08/2026_08_26_cli-owns-the-read/{plan,phase-3,phase-6,measurements}.md` |
| Unchecked     | Task 2's "the journal is owner-only" — `fix`      |
| Unplanned     | `.github/workflows/cli-ci.yml` (not in the projection, and broken by the deletion — the critical finding); `scripts/__tests__/aidd-telemetry-switch-gitignore.test.js` deleted on the executor's own judgement (defensible: its subject is the file task 3.1 removes, and all six of its claims are re-proven in `telemetry-on-runs-privacy.e2e.test.ts`, verified 1:1 here); `aidd-telemetry-cost-skill.test.js`, `plugin-install-shape.test.js`, `plugin-asset-translation.unit.test.ts`, `telemetry-plugin-standalone.e2e.test.ts`, `telemetry-lifecycle.e2e.test.ts` edited without being named in the projection (each traces to the deletion; the two claims that changed level or died are findings above); `plan.md` gained two Decisions and `phase-3.md` gained task 0 after the fact — declared, argued, and consistent with what shipped |
| Environment   | `rtk` absent from `PATH` (`which rtk` → not found); every `grep`/`find`/`diff` in this review ran as the plain command |
