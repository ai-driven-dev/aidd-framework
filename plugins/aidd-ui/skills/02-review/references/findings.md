# Finding severity

| Severity | Threshold |
| --- | --- |
| `critical` | prevents task completion, safe recovery, or essential access |
| `warning` | materially increases error, effort, ambiguity, or exclusion |
| `minor` | creates a bounded consistency or refinement defect without blocking the task |

Order findings by user consequence within each severity. Use one row per independent defect.
