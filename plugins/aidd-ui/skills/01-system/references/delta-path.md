# System delta path

1. Reuse the task folder containing the supplied requirement or UI contract.
2. Otherwise use an explicit caller supplied task folder.
3. Otherwise create `aidd_docs/tasks/<yyyy_mm>/<yyyy_mm_dd>_<requirement-slug>/`.

Write the delta there as `system-delta-<system-id>-<delta-id>.md`.
